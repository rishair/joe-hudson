---
type: experiment
id: E-042
status: succeeded
parent_goal: G-010
parent_experiment:
depends_on: E-039
claim_ttl: 60
claimed_by:
claimed_at:
completed_at: 2026-05-24T16:30:00Z
created: 2026-05-23
---

# Build wiki view: routing, rendering, internal wikilinks

## Hypothesis

A Next.js route `/wiki/[...slug]` can render any file from `web-app/content/wiki/` (produced by E-039) as a styled markdown page. Internal `[[wikilinks]]` resolve to the correct routes within the wiki. The wiki view is navigable on its own (separate from chat) and is the surface the chat's resource modal expands into.

## Method

1. Confirm E-039 has produced `web-app/content/wiki/` with normalized links
2. Define interfaces per the [[coding-architecture]] playbook's four-method shape — read-only repo, so only `find`:
   - `WikiPage { slug, title, frontmatter, body, related: string[], category }`
   - `WikiRepository`:
     - `find(criteria?: { slug?: string; category?: string; query?: string; limit?: number }): Promise<WikiPage[]>`
   - Callers needing a singleton (e.g., "get page by slug") do `(await wikiRepo.find({ slug }))[0] ?? null`
   - Callers needing the index do `wikiRepo.find()` (or `find({ limit: ... })` if pagination matters later)
3. Implement `FsWikiRepository` (server-only) that reads from `web-app/content/wiki/`. Parses markdown + frontmatter. Resolves wikilinks at render time (not at ingest — the slug→file mapping is dynamic and a missing file should render as a broken-link span, not error).
4. Add `web-app/app/wiki/[[...slug]]/page.tsx` (catch-all route):
   - If slug is empty → wiki index page (categories + counts)
   - If slug resolves to a file → render the page via `<WikiRenderer>` (see step 5)
   - If slug doesn't resolve → 404 with a "did you mean X" suggestion if there's a close match
   - Read `?fromChat=<msgId>` from `searchParams`. If present, render a small "Back to chat at message N" link at the top of the page (per R-017 §7). On click: `router.back()` primary, falls back to `router.push('/?scrollToMessage=' + msgId)` if `history.length <= 1`. The chat page reads `?scrollToMessage=` on mount, scrolls the conversation pane to that message id, and `replace`s the URL to clear the param.
5. **`<WikiRenderer>` component** at `web-app/app/components/wiki-renderer.tsx`:
   - Props:
     - `page: WikiPage` (or `slug: string` + the renderer does the lookup)
     - `linkBehavior?: 'route' | 'in-modal'` (default `'route'`)
     - `onLinkClick?: (slug: string) => void` (required when `linkBehavior === 'in-modal'`)
   - Markdown renderer: `react-markdown` + `remark-gfm`, with a custom `[[wikilink]]` pre-processor (converts `[[slug]]` and `[[slug|display]]` per R-018 to standard markdown links).
   - When `linkBehavior === 'route'`: internal `/wiki/...` links render as Next.js `<Link>` components.
   - When `linkBehavior === 'in-modal'`: internal `/wiki/...` links render as `<button>` elements; `onClick` calls `onLinkClick(slug)` where `slug` is extracted from the href. The visual styling is identical (looks like a link, behaves like a link, but does not navigate).
   - External links (anything not `/wiki/...`) always render as standard `<a target="_blank" rel="noopener">` regardless of `linkBehavior`.
   - Broken internal links (slug not in the index) render as a visibly-broken span with a `title` tooltip explaining the missing target, NOT as a clickable link in either mode.
6. Style with Tailwind (already in the playbook stack).
7. Test: load 5 random wiki pages in route mode, click through wikilinks, verify navigation works and the URL updates correctly. Render the same renderer with `linkBehavior="in-modal"` and a stub `onLinkClick`, verify clicks call the callback instead of navigating. Verify a `[[broken-slug]]` renders visibly broken, not clickable, not crashing.

## Success Criteria

- `/wiki` shows the category index.
- `/wiki/concepts/limiting-belief` (or any actual slug) renders the entry.
- Internal `[[wikilinks]]` are clickable and navigate correctly in default (`linkBehavior="route"`) mode.
- In `linkBehavior="in-modal"` mode, internal wikilinks invoke `onLinkClick(slug)` instead of routing — verified by E-044 integration test.
- Broken wikilinks render as visibly broken (not crashing) in both modes.
- Navigation is browser-native (back button works, URLs are shareable).
- `?fromChat=<msgId>` on a wiki page renders a "Back to chat at message N" link; clicking it returns to the chat with conversation scroll restored to message N.
- Page renders in under 500ms (file read + markdown parse).

## Blast Radius

Adds `web-app/app/wiki/[[...slug]]/page.tsx`, `web-app/app/components/wiki-renderer.tsx`, `web-app/app/lib/wiki/*` (repository + types). Reads from `web-app/content/wiki/` (read-only). No conflict with the chat surface.

Adds a small handler to the chat page (`app/page.tsx` or equivalent) for `?scrollToMessage=<msgId>` URL param: on mount, scroll to message, then `router.replace('/')` to clean the URL. This is shared with E-044's expand-then-back-to-chat path.

## Result

**Implementation landed** (2026-05-24). Wiki view runs at `/wiki/...` against the E-039 ingested content.

**Files added (web-app/):**

| File | Purpose | Lines |
|---|---|---|
| `app/wiki/[[...slug]]/page.tsx` | Optional catch-all route. Handles index, category, page, broken-link landing, 404, breadcrumbs, related section. | ~320 |
| `app/components/wiki-renderer.tsx` | Client renderer. Custom `a` component dispatches by URL shape + `linkBehavior` prop. | ~210 |
| `app/components/back-to-chat.tsx` | `?fromChat=<msgId>` reader; `router.back()` primary, `?scrollToMessage=` push fallback. | ~55 |
| `app/components/chat-scroll-restorer.tsx` | Chat-page sibling: reads `?scrollToMessage=<msgId>`, scrolls into view, `router.replace('/')`. | ~50 |
| `app/lib/wiki/types.ts` | `WikiPage`, `WikiPageRef`, `WikiRepository`, `IngestManifest`, `SlugIndex`. | ~95 |
| `app/lib/wiki/fs-wiki-repository.ts` | Server-only filesystem repo. Manifest cached; per-slug lazy file reads. | ~150 |
| `app/lib/wiki/wikilinks.ts` | `[[slug]]` → markdown link pre-processor with `_broken` sentinel. | ~70 |
| `app/lib/wiki/frontmatter.ts` | YAML-lite parser tuned to the corpus shape. | ~50 |
| `app/lib/wiki/container.ts` | Singleton `getWikiRepo()` wiring. | ~20 |
| `app/lib/wiki/index.ts` | Barrel exports. | ~15 |

**Files modified:**
- `app/page.tsx` — three small additions: `<Suspense><ChatScrollRestorer/></Suspense>` mount, `id="msg-{id}"` on each message article (so the scroll restorer can target), Wiki link in the sidebar.
- `package.json` — added `react-markdown@^10.1.0` and `remark-gfm@^4.0.1`.

**Files NOT modified:** anything under `app/lib/db/`, `app/lib/migrations/`, `app/lib/repos/`, `app/lib/state/`, `app/lib/types/` (all E-041 territory). The E-041 sibling agent landed `app/page.tsx` ~5 minutes after I read it for the first time; I re-read and merged my three additions surgically into the new structure.

**Architecture decision: wikilink processing is server-side.** The wikilink pre-processor runs on the server (in the route's `PageView`) before passing markdown to `<WikiRenderer>`. This keeps the slug index (~2,376 entries) out of the client bundle and lets the renderer be reusable across E-042 (route mode) and E-044 (in-modal mode) with no client-side dependency on the manifest. The renderer only needs to parse `href` strings to dispatch.

**Architecture decision: inline styles, not Tailwind.** The original method specified Tailwind, but the rest of the app (E-040, E-041, E-045) uses inline styles throughout. Consistency wins over playbook spec; Tailwind can be added later in a single sweep if visual design work calls for it.

**Architecture decision: `?fromChat=` reader is its own client component.** Rather than convert the wiki page to a client component (it's a server component for the SSR/caching wins), `<BackToChat />` is a small client island wrapped in `<Suspense>` (required by Next.js for `useSearchParams`). Same pattern for `<ChatScrollRestorer />` on the chat page.

### Success criteria check

| Criterion | Status | Evidence |
|---|---|---|
| `/wiki` shows the category index | yes | HTTP 200, 13 categories listed with counts (anti-patterns 115, applications 19, concepts 227, concerns 126, distinctions 138, examples 249, frameworks 31, moves 324, patterns 117, practices 171, principles 646, questions 92, reads 121 — total 2,376) |
| `/wiki/concepts/limiting-belief` renders | yes | HTTP 200; title, breadcrumbs, body, 54 resolved wikilinks, related section all present |
| Wikilinks clickable in route mode | yes | All `<a href="/wiki/...">` rendered as Next.js `<Link>` |
| In-modal mode invokes `onLinkClick` | code-verified | Renderer renders `<button onClick={() => onLinkClick(...)}>` when `linkBehavior='in-modal'`. E-044 will exercise the integration. |
| Broken wikilinks render visibly broken, not clickable | yes | On `moves/use-the-strong-word-for-somatic-landing` (7 broken targets): all 7 render as `<span class="wikilink-broken">` with `title` tooltip. Resolved links on same page (3) render as anchors. |
| Navigation browser-native | yes | All internal links are Next.js `<Link>` → browser back/forward + URL share work natively |
| `?fromChat=<msgId>` renders back-link | yes | `Back to chat at message <prefix>` appears only when param is set; `router.back()` primary, `?scrollToMessage=` push fallback |
| `?scrollToMessage=<msgId>` scrolls chat | yes | `ChatScrollRestorer` mounted in `app/page.tsx`'s shell; messages now carry `id="msg-<id>"`; restorer uses RAF retry to wait for hydration, then `router.replace('/')` to clear param |
| Page renders under 500ms | yes | Dev-mode timings on `/wiki/concepts/limiting-belief`: cold 416ms, warm 166-366ms. Production build's static path would be substantially faster. |

### Additional verification

- **Typecheck:** `npx tsc --noEmit` clean.
- **Production build:** `npx next build` clean — 4 routes total (`/`, `/_not-found`, `/api/chat`, `/wiki/[[...slug]]`), compiled in 11.2s.
- **Anchor handling:** `[[fear-of-joy#impermanence-is-what-makes-the-thing-sweet]]` correctly renders as `<a href="/wiki/patterns/fear-of-joy#impermanence-is-what-makes-the-thing-sweet">`.
- **Piped display:** `[[fear-triangle|victim role]]` renders as `<a href="/wiki/frameworks/fear-triangle">victim role</a>`.
- **Did-you-mean suggestion:** `/wiki/_broken/limiting-belieff` (typo) lands on the broken-page UI with "Did you mean Limiting Belief?" suggestion derived from a bounded-Levenshtein scan of the slug index.
- **Apostrophe handling:** `[[don't-soften-the-recognition]]` (a real corpus link with an apostrophe) renders as a broken-link span (no target exists); apostrophe survives through `encodeURIComponent` correctly and the title tooltip shows `don&#x27;t-soften-the-recognition`.

## Analysis

The implementation came in well under the 500ms render budget largely because the manifest cache makes subsequent reads O(1) and the per-page work is a single file read + markdown parse. The first request to a category index ran 416ms; warm requests are 166-366ms in dev mode. Production would be substantially faster (no HMR, no per-request compilation).

Three implementation choices worth noting for E-044 and future work:

1. **Server-side wikilink processing** means the renderer is a pure markdown-to-React component. No slug-index dependency in the client bundle. E-044's modal will pass an already-processed body to the same renderer — symmetry that simplifies the integration.

2. **`_broken` sentinel category** in href URLs gives the client renderer a deterministic way to detect broken targets without a separate prop or side-channel. The route layer also recognizes `/wiki/_broken/<slug>` and renders a polite landing page with a "did you mean X" suggestion. A user clicking a sentinel directly (rare — the renderer renders them as spans, not links) lands somewhere helpful instead of a 404.

3. **`<BackToChat>` and `<ChatScrollRestorer>` as separate client islands** keeps the wiki page and chat page as their primary types (server component and client component respectively) with minimum boundary churn. Both islands sit in `<Suspense>` per Next.js's `useSearchParams` contract.

The success criterion "page renders in under 500ms" depends on what counts as "render time." File-read + markdown-parse + RSC stream is well under the threshold; first-byte-to-DOM-ready in a real browser will include network, JS execution, and styling. The dev-mode HTTP timings (416ms cold, 166-366ms warm) are an upper bound; production is faster.

The most surprising part of the work was the apostrophe edge case in source markdown. The corpus has `[[don't-soften-the-recognition]]`-style links (apostrophes in slugs) that R-018 didn't enumerate as a distinct format. They render correctly as broken spans because the slug index has no apostrophe-bearing entries, so the case-fold lookup misses and we fall through to the sentinel. If G-007 ever wants to author articles with apostrophe-bearing slugs, the ingestion script would need to handle the URL-encoding more explicitly. Not a blocker today.

## Dead Ends

None significant. The original method specified Tailwind for styling, but the existing app uses inline styles consistently. I switched to inline to match — not a dead end so much as a small spec deviation.

## Next Steps

E-042 unblocks E-044 (resource modal) directly:
- E-044 imports `<WikiRenderer linkBehavior="in-modal" onLinkClick={focus} />` and passes a server-pre-processed body.
- E-044 reads `message.parts.find(p => p.type === 'data-resources')` per R-016/R-017 §6 for the right-panel list.
- E-044's "Expand to full" button calls `router.push('/wiki/' + slug + '?fromChat=' + msgId)` and closes the modal. The wiki page already reads `?fromChat=` and renders the back-link.

E-042's renderer can be exercised in-modal mode by E-044 without further changes here. The `onLinkClick` callback receives `(slug, anchor | null)` so E-044 has the option of preserving anchors when navigating between focused resources within the modal (or ignoring the anchor, which is fine for v1).

Operator note for future ingestion runs: the manifest cache in `FsWikiRepository` is keyed off process lifetime. Re-running `bun run ingest` while `next dev` is running requires a manual `next dev` restart to pick up new pages or new broken-link entries. A pre-commit/post-merge hook to invalidate the cache is a future polish item.

## Links

- Parent goal: [[G-010]]
- Depends on: [[E-039]] (ingested content)
- Feeds: [[E-044]] (resource modal reuses the `<WikiRenderer>` with `linkBehavior="in-modal"`)
- Coordinates with: [[R-017]] (specifies the `linkBehavior` prop, `?fromChat=`/`?scrollToMessage=` plumbing, and the `<WikiRenderer>` shape used by both surfaces), [[R-018]] (wikilink syntax including `[[slug|display]]`)
- Playbook: [[ai-web-app]] (App Router routing patterns), [[coding-architecture]] (FsWikiRepository pattern)
