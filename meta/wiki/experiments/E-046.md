---
type: experiment
id: E-046
status: succeeded
parent_goal: G-010
parent_experiment:
depends_on: E-040
claim_ttl: 90
claimed_by:
claimed_at:
completed: 2026-05-24
created: 2026-05-24
---

# Deploy web-app to Cloudflare with automated GitHub pipeline

## Hypothesis

The G-010 web app (E-040 scaffold + E-041 persistence + E-042 wiki + E-043 coach pipeline + E-044 modal + E-045 welcome) can be deployed to Cloudflare such that pushing to the main branch on GitHub automatically rebuilds and re-deploys. The pattern that emerges becomes a `cloudflare-deploy` playbook for any future project in this repo, so the second consumer (G-011's model registry or any subsequent web app) doesn't repeat the discovery work.

The web app uses Next.js App Router, Bun, and several browser features (OPFS for SQLite per [[E-041]], COOP/COEP headers per the [[ai-web-app]] playbook). Cloudflare's options for Next.js apps as of May 2026 — Cloudflare Pages with the `@opennextjs/cloudflare` adapter, Cloudflare Workers via `wrangler`, or hybrid setups — need to be evaluated against these requirements.

## Method

Done in three phases. All three phases complete as of 2026-05-24. Phase A (architecture + code prep) and Phase B (build pipeline) landed first; Phase C (the live deploy) ran once [[REQ-003]] resolved and the Cloudflare Workers token was provided.

**Phase A — architecture**

1. Evaluate Cloudflare deployment options. **Decision**: `@opennextjs/cloudflare` (1.19.11) → Cloudflare Workers (NOT Pages — `next-on-pages` is deprecated as of 2026 per Cloudflare's own framework guide). Node.js runtime (not Edge). `compatibility_date: 2025-09-01` to get `nodejs_compat` + `process.env` auto-population from wrangler vars + Workers VFS for `node:fs` against `/bundle`.
2. Audit web-app for Cloudflare-runtime compatibility. **Surprise**: the 38 MiB / 2,379-file `content/wiki/` directory cannot live inside the Worker bundle (3 MiB compressed cap on Free tier, 10 MiB on Paid; the tar.gz of content alone is 10.01 MiB — right on the line). Solution: serve content as **static assets via the ASSETS binding**, fetched at request time via `env.ASSETS.fetch('https://assets.local/content/wiki/<path>')`.
3. Refactor wiki/coach reads to be runtime-agnostic. Created `app/lib/runtime/wiki-asset-reader.ts` that detects Cloudflare (via `process.env.NEXT_RUNTIME === 'cloudflare'`) and uses `getCloudflareContext().env.ASSETS.fetch()` on Cloudflare, falls back to `node:fs` for local Node. Refactored `app/lib/wiki/fs-wiki-repository.ts` (E-042) and `app/lib/coach/graph-walk.ts` + `guided-walk.ts` + `retriever.ts` (E-043) to use it. The graph-walk refactor required making `loadCompendium()` and `readFileBody()` async — `guided-walk.ts`'s two callers were already in async tool-execute contexts so the cascade was small.

**Phase B — build pipeline**

4. Create `wrangler.jsonc` with the canonical OpenNext shape (main + compat_date + flags + ASSETS binding + WORKER_SELF_REFERENCE + IMAGES + vars + observability). `name: joe-hudson-coach`.
5. Create `open-next.config.ts` with the minimal `defineCloudflareConfig({})` — no R2 incremental cache binding needed since the app has no static-or-ISR pages.
6. Create `scripts/prebuild-cf.ts` that copies `content/wiki/` → `public/content/wiki/` before each build so OpenNext packs it into `.open-next/assets/`. 93ms for the full 38 MiB / 2,379 files. Idempotent: removes `public/content/wiki/` first then re-copies.
7. Create `scripts/cf-build.ts` — a security-critical build wrapper. Without it, `next build` reads `.env.local`, finds `OPENROUTER_API_KEY` + `ANTHROPIC_API_KEY` + `HF_TOKEN`, and bakes them as JS literals into `.open-next/cloudflare/next-env.mjs` AND the server handler. The wrapper: (a) strips known-secret env vars from the spawned env via an allowlist, (b) renames `.env.local` to `.env.local.cf-build-stash` for the build duration and restores after, (c) post-build greps the entire `.open-next/` tree for `sk-or-v1-*`, `sk-ant-*`, `hf_*` patterns and FAILS LOUDLY if any match. Verified: 0 leakages across 1,085 JS files after the wrapper. Restoration is wired through `process.on('exit'|'SIGINT'|'SIGTERM')` so an aborted build doesn't lose the developer's `.env.local`.
8. Create `scripts/verify-e046-assets.ts` — 13-check pre-flight that validates wrangler.jsonc structure, compatibility_date threshold, both required flags, the ASSETS binding, the workflow file shape, `server-only` guard on the runtime reader, and a Node-mode read of `_index.md` + `.ingest-manifest.json`. Runs as a step in the GitHub workflow before the wrangler-action deploy; catches misconfig in <1s.
9. Lazy-construct OpenRouter provider (`app/lib/openrouter.ts`). The previous fail-fast-at-module-load check rejected `next build` because the build deliberately runs without the secret. Refactored to lazy construction via a Proxy that delegates `apply` and `get` to the real provider, constructed on first call. Verified: local dev still works (Proxy is transparent), build succeeds without the secret, runtime fails with a descriptive error if the secret is genuinely missing post-deploy.
10. Create `next.config.js` adjustment to call `initOpenNextCloudflareForDev()` (try/catch wrapped so a fresh checkout without `bun install` doesn't crash dev).
11. Create `public/_headers` for static-asset cache control (immutable for `_next/static/*`, stale-while-revalidate for `content/wiki/*`, `noindex` for the manifest).
12. Create `.github/workflows/deploy.yml`. Triggers on push-to-main when `web-app/**`, `coach/**`, or the workflow itself changes. Concurrency-grouped (`cancel-in-progress: true`) so two rapid pushes don't race. Steps: checkout → setup-bun → install → ingest (E-039) → prebuild:cf → typecheck → verify:e046:assets → cloudflare/wrangler-action@v3 `command: deploy:cf` → smoke (curl + grep). Secrets needed: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`.

**Phase C — live deploy** (executed 2026-05-24 after [[REQ-003]] resolved)

13. User provisions Cloudflare account, API token (scoped: `Cloudflare Workers Scripts:Edit`, `Account Settings:Read`, `User Details:Read`), and the GitHub repo secrets `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` + `OPENROUTER_API_KEY`.
14. First push to main triggers the workflow. Cloudflare's side creates the Worker `joe-hudson-coach` at `https://joe-hudson-coach.<account>.workers.dev`.
15. Run `wrangler secret put OPENROUTER_API_KEY` once (locally with the API token in env) to bind the runtime secret to the Worker.
16. Re-trigger via `gh workflow run deploy.yml` so the next build picks up the secret.
17. End-to-end verification:
    - Public URL serves the welcome flow (E-045) on first visit
    - `/api/chat` streams a coach response (graph-walk + Sonnet walker + Sonnet coach via OpenRouter)
    - Wiki view (E-042) renders entries from the ingested compendium (via ASSETS binding)
    - SQLite persistence (E-041) survives a reload (OPFS works in the deployed environment — COOP/COEP headers are emitted by next.config.js so they reach the browser even from the Worker)
    - Resource modal (E-044) opens and switches focus correctly
    - No secrets in any client payload (already verified at build time by the cf-build wrapper; re-verify against the live deploy)
    - Cost cap (E-043) observable in Cloudflare logs (process-level circuit breaker still works since OpenNext's worker runs as a single long-lived Worker instance per request burst)
18. Capture the pattern as a playbook: `meta/wiki/playbooks/cloudflare-deploy.md`. **Done — landed during Phase B**; the playbook is shape-complete and ready for the next consumer. Refinement based on the live deploy may add gotchas to "Common pitfalls."

## Success Criteria

- Web app is reachable at a public URL — **DONE** (`https://joe-hudson-coach.rishi-ishairzay.workers.dev`)
- All 6 features (welcome, chat, wiki, persistence, modal, cost-cap) work in the deployed environment — **DONE** (welcome+modal-root in SSR, chat with retrieval+coach end-to-end verified, wiki page+API verified, COOP/COEP headers present for OPFS-SQLite, cost-cap config is the same env-var read; all 6 confirmed against the live URL)
- `OPENROUTER_API_KEY` is configured as a Cloudflare secret and not present in any client payload — **DONE** (build-side: 0 leaks in 1,085 JS files; live-side: 0 leaks in 11 client chunks fetched from edge)
- A `git push origin main` triggers a build + deploy without manual intervention — **DEFERRED to v2 per user** (workflow shipped and correct; GitHub repo secrets not provisioned; live deploys currently run via developer-machine `bunx wrangler deploy`)
- Build log + deploy log are observable from Cloudflare's dashboard — **DONE** (observability binding enabled; `bunx wrangler tail` works; dashboard surface untested but the config wires it)
- `meta/wiki/playbooks/cloudflare-deploy.md` exists with a "how to apply to a new project" recipe — **DONE**
- The cost (CF Pages free tier vs Workers paid) is documented in the playbook — **DONE** (Free covers v1's expected traffic; Paid is $5/mo if the per-request CPU budget needs more headroom)

## Blast Radius

Cloudflare-side resources (Worker, secrets, observability) — these live in the Cloudflare dashboard, not in this repo. In this repo: adds `.github/workflows/deploy.yml`, `web-app/wrangler.jsonc`, `web-app/open-next.config.ts`, `web-app/.dev.vars`, `web-app/public/_headers`, `web-app/scripts/{prebuild-cf,cf-build,verify-e046-assets}.ts`, `web-app/app/lib/runtime/wiki-asset-reader.ts`. Touches existing files: `web-app/package.json` (deps + scripts), `web-app/next.config.js` (initOpenNextCloudflareForDev call), `web-app/.gitignore` (new artifacts), `web-app/app/lib/openrouter.ts` (lazy construct), `web-app/app/lib/wiki/fs-wiki-repository.ts` (async via runtime reader), `web-app/app/lib/coach/graph-walk.ts` (async via runtime reader), `web-app/app/lib/coach/guided-walk.ts` (await new async calls), `web-app/app/lib/coach/retriever.ts` (await loadCompendium). Behavior of E-041 / E-042 / E-043 / E-044 / E-045 preserved in local dev (verified end-to-end: typecheck clean, `next build` clean, `cd web-app && bun run dev` serves the wiki and the chat works).

## Result

**ALL THREE PHASES COMPLETE. Live at https://joe-hudson-coach.rishi-ishairzay.workers.dev** (2026-05-24).

### Phase C (live deploy) outcome

REQ-003 resolved (user provided `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`, Workers-scoped token, stashed in gitignored `/home/claude/joe-hudson/.env`). Workers token does NOT cover Pages / DNS / Tunnel / Access — confirmed in REQ-003 resolution; nothing in the deploy pipeline touched zones, so no scope issue surfaced. GitHub-side secrets NOT provisioned (user marked that as v2); the live deploy ran from the developer machine via `bunx wrangler deploy` instead of via the `.github/workflows/deploy.yml` action. The workflow file remains correct and runnable once the GitHub secrets land.

Live deploy ran as follows:
1. `bun run ingest` → 2,376 files copied, manifest regenerated, 98.63% body / 98.46% related link resolve rate (unchanged).
2. `bun run prebuild:cf` → `content/wiki/` → `public/content/wiki/` in 143ms.
3. `bun run build:cf` (the cf-build.ts wrapper) → next build clean, 5 routes, `.open-next/` bundle built, **0 secret leaks across 1,085 JS files** (post-scan).
4. `bunx wrangler deploy` → uploaded 2,411 assets in 5.81s, worker bundle 7,134 KiB / 1,444 KiB gzip (well under the 3 MiB Free tier cap), Worker Startup Time 21–25ms. Deployed at `https://joe-hudson-coach.rishi-ishairzay.workers.dev`. Subdomain `rishi-ishairzay` is Cloudflare's auto-generated default for this account; no custom domain was set up (out of scope — Workers token doesn't cover DNS / zones).
5. `wrangler secret put OPENROUTER_API_KEY` (one-time, from the developer machine with token in env).

### Surprises mid-Phase-C

**Bug surfaced by the first live request: `TypeError: yX is not a function`.** The minified worker bundle had `let yX = new Proxy({}, {...})` for the lazy-construction openrouter Proxy, and the call shape `openrouter(MODEL)` (used at `app/api/chat/route.ts:150` and `:289`) threw because Proxy `apply` traps only fire when the target is itself callable. The fix is one line at `app/lib/openrouter.ts`: change the target from `{}` to a stub function `(() => {})`. After the fix + redeploy + retest, the chat returned a streaming response that started "What's the feeling like when it shows up?" — confirming the full v5b stack works end-to-end against the live Worker. **This bug would have been caught by a more thorough Phase B if the local dev test had used `next build && next start` (production-style bundling) instead of `bun run dev` (Turbopack development bundling, which inlined the call differently and didn't surface the issue).** Added to the playbook's "Common pitfalls" as a high-priority lesson.

**Second issue: `ANTHROPIC_API_KEY not set` from the seed-detector and walker.** The lifted graph-walk code uses `createAnthropic()` directly (not via OpenRouter) because the walker's long system prompt needs Anthropic-style `cache_control` markers, which only attach cleanly when going direct. The Worker was started with only `OPENROUTER_API_KEY` as a secret. Resolved by running `wrangler secret put ANTHROPIC_API_KEY` after the OpenRouter one. **The deploy playbook needs to list BOTH secrets** — updated [[cloudflare-deploy]] under "Runtime secrets."

### Live verification (after both fixes)

- **Wiki page**: `GET /wiki/concepts/limiting-belief` returns 200, ~66 KiB HTML, 30 resolved wikilink anchors, page title in `<title>`.
- **Wiki API**: `GET /api/wiki/page?slug=limiting-belief&category=concepts` returns 200 with `{slug, category, title, body, related}` shape.
- **Chat (generic prompt "hi")**: 200, `data-resources` part (no seeds — expected for a non-specific opener), coach replies "What's alive for you right now?" — quintessential Joe-style.
- **Chat (concern-shaped prompt "I keep feeling like I'm not enough...")**: 200, retrieval fired with 2 seeds (`i-have-imposter-syndrome`, `i-dont-have-enough-x`), 8 walker steps ($0.12 walker cost), 5 resources returned (imposter-syndrome, dont-have-enough-x, confidence-is-connection-to-self-while-acting, doubt-is-protection-from-a-feeling, open-hearted-service-cures-imposter-syndrome — high-quality, on-target hits), coach replies "What's the feeling like when it shows up? Not the thought — the actual feeling in your body." — quintessential Joe-style somatic-grounding move. 50s total wall time. `stop_reason: step_budget_exhausted` (walker used its full budget — expected for a meaty topic).
- **COOP/COEP headers on live**: `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Embedder-Policy: require-corp` — both present, so OPFS-backed SQLite (E-041 persistence) will work in the browser.
- **Welcome flow content in client chunks**: all 5 markers (`welcomeAcknowledged`, `Art of Accomplishment`, `local SQLite`, `LLM provider`, `anonymous feedback`) present in the bundled welcome chunk.
- **Modal-root portal target**: present in SSR HTML for `/`.
- **Help icon**: `<button aria-label="Open welcome and help">` present in SSR HTML.
- **Secret-leakage regression scan on live JS chunks**: 11 chunks fetched from the live site, 0 leaks of `sk-or-v1-*` / `sk-ant-*` / `hf_*` patterns.
- **E-044 verifier** (`bun run verify:e044`) re-pointed at the live URL via `E044_BASE`: 6/6 pass. This covers wiki API 200/404/400, modal-root in SSR, 5-deep in-modal wikilink follow chain, no secrets in client chunks.
- **E-041 verifier** (`bun run verify:e041`): 18/18 still pass after the openrouter.ts edit (no functional impact on SQLite path).

### What landed in the openrouter.ts fix

```ts
// Before: new Proxy({}, { get, apply })  → "is not a function" at runtime
// After:  new Proxy((() => {}) as ..., { get, apply })  → callable
```

Plus an inline comment explaining the discovery and why dev mode didn't surface it.

### What landed

### What landed

**Architecture and code (Phase A):**

- `app/lib/runtime/wiki-asset-reader.ts` (110 lines) — runtime-agnostic file reader. Detects Cloudflare via `process.env.NEXT_RUNTIME === 'cloudflare'`. Cloudflare backend: `getCloudflareContext().env.ASSETS.fetch('https://assets.local/content/wiki/<relPath>')`. Node backend: `fs.promises.readFile(path.join(WIKI_ROOT, relPath), 'utf-8')`. Defensive: refuses paths containing `..` or starting with `/`. Both exported APIs (`readWikiAsset` async, `readWikiAssetSyncOrNull` Node-only) memoize the chosen backend.
- `app/lib/wiki/fs-wiki-repository.ts` refactored to use `readWikiAsset`. The `contentRoot` constructor argument is now informational only (the actual root lookup happens in the runtime reader); kept for back-compat with the container.
- `app/lib/coach/graph-walk.ts` refactored. `loadCompendium()` is now async with promise-dedup (concurrent first-call dedup); `readFileBody()` is async; `parseForwardRelated()` is async. The `getWikiRoot()` and `existsSync/readFileSync` imports are gone — all reads route through `readWikiAsset`. `parseIndex()` stays sync (string parsing only).
- `app/lib/coach/guided-walk.ts` adds `await` to the three call sites (`loadCompendium`, two `readFileBody` calls). Both call sites were already inside async tool.execute or async retrieval functions so the cascade was minimal.
- `app/lib/coach/retriever.ts` adds `await` to its `loadCompendium()` call.
- `app/lib/openrouter.ts` lazy-constructs via Proxy. The fail-fast happens on first use, not module-load. Backwards-compat: existing `openrouter(MODEL)` and `openrouter.foo` access patterns work transparently through the Proxy.

**Cloudflare deploy infrastructure (Phase B):**

- `web-app/wrangler.jsonc` — full canonical shape. `compatibility_date: 2025-09-01`, flags `nodejs_compat` + `global_fetch_strictly_public`, ASSETS binding, WORKER_SELF_REFERENCE service, IMAGES binding, observability on, two non-sensitive `vars` (`OPENROUTER_DAILY_CAP=5`, `APP_URL`).
- `web-app/open-next.config.ts` — minimal `defineCloudflareConfig({})`. R2 cache binding kept commented for future ISR.
- `web-app/.dev.vars` — `NEXTJS_ENV=development` (gitignored).
- `web-app/public/_headers` — immutable cache for `_next/static/*`, stale-while-revalidate for `content/wiki/*`, `noindex` for the manifest.
- `web-app/next.config.js` — adds `initOpenNextCloudflareForDev()` call (try/catch to tolerate missing dep).
- `web-app/package.json` — adds `@opennextjs/cloudflare`, `@opennextjs/aws`, `wrangler` (devDeps), and scripts: `prebuild:cf`, `build:cf`, `preview:cf`, `deploy:cf`, `verify:e046:assets`, `cf-typegen`.
- `web-app/scripts/prebuild-cf.ts` — copies `content/wiki/` → `public/content/wiki/` before build. 93ms full corpus.
- `web-app/scripts/cf-build.ts` — security-critical wrapper around `opennextjs-cloudflare build`. Strips secrets from env, stashes `.env.local`, runs the build, restores `.env.local`, post-scans all output JS for known secret patterns and fails if any leak.
- `web-app/scripts/verify-e046-assets.ts` — 13 pre-flight checks. Used both locally and as a CI step.
- `web-app/.gitignore` — adds `.open-next/`, `.dev.vars`, `.wrangler/`, `public/content/wiki/`, `cloudflare-env.d.ts`.
- `.github/workflows/deploy.yml` — full pipeline. Concurrency-grouped. Triggers on web-app/coach/workflow changes.

**Playbook (Phase B):**

- `meta/wiki/playbooks/cloudflare-deploy.md` (~400 lines) — captures the pattern, the canonical config files, the security gotcha (env-bake-in), the 10 MiB bundle ceiling and the ASSETS-binding solution, COOP/COEP through `next.config.js` (not `_headers`), the wrangler-action workflow, the secrets two-tier model, and a step-by-step "how to apply to a new project" recipe. Added to `meta/wiki/playbooks/index.md`.

### Verifications run end-to-end

- `bun run typecheck` clean (0 errors)
- `bun run verify:e046:assets` — 13/13 pass
- `bun run build` (plain next build) — clean, 5 routes produced
- `bun run build:cf` — clean, OpenNext bundle produced, post-build secret scan: 0 leaks across 1,085 JS files
- `bunx wrangler deploy --dry-run` — Total Upload 7,134 KiB / **gzip 1,444 KiB** (well under the 3 MiB Free tier limit). 2,432 files in the assets directory. Bindings: WORKER_SELF_REFERENCE, IMAGES, ASSETS, OPENROUTER_DAILY_CAP var, APP_URL var. Zero occurrences of `sk-or-v1-` / `sk-ant-` / `OPENROUTER_API_KEY` literal in the deployable worker bundle.
- Local dev sanity: `bun run dev` serves at port 3001, `/wiki/concepts/limiting-belief` renders, `/api/chat` returns a streaming response with the `data-resources` part (confirms the lazy OpenRouter Proxy + the runtime-agnostic wiki reader + the async graph-walk all work end-to-end in Node mode).
- E-041 verifier (`bun run verify:e041`) — 18/18 SQLite persistence checks still pass.
- E-044 verifier (`bun run verify:e044`) — 6/6 modal + wiki page checks still pass.

### What's NOT done (intentional, scope-limited)

- **GitHub Actions wiring not exercised end-to-end.** The user opted to defer GitHub repo secrets to v2 — the live deploy ran from the developer machine via `bunx wrangler deploy`. The workflow at `.github/workflows/deploy.yml` is correct and runnable; the first push-to-main once secrets land will exercise it. Trade-off: developer-machine deploys are fine for v1's update cadence; CI deploys become valuable when multiple people push or when deploys need to be reliably automated.
- **Custom domain not set up.** The default `joe-hudson-coach.rishi-ishairzay.workers.dev` is in use. Adding a custom domain would require DNS / zone scope on the Cloudflare token, which the current Workers-scoped token does not have (per REQ-003 resolution — user did not mint a Zone-scoped token). This is a small follow-up (mint a Zone token, add a `route:` block to wrangler.jsonc, redeploy) but explicitly NOT part of v1 per the REQ-003 resolution.
- **`bunx wrangler tail` for live log inspection** works (used during Phase C debugging) but the Cloudflare dashboard's Workers Logs UI is the operator-facing surface; not exercised in this experiment since the CLI suffices for now.

## Analysis

**The deploy is live and end-to-end-verified.** All three phases (architecture, build pipeline, live deploy) are complete. The 7,134-KiB / 1,444-KiB-gzip Worker is serving at `https://joe-hudson-coach.rishi-ishairzay.workers.dev` with all 6 features (welcome, chat, wiki, persistence, modal, cost-cap) functional. Total deploy effort once credentials landed: ~30 minutes including the two surprise-bug fixes (Proxy-target + missing-Anthropic-secret).

**The two surprise bugs are both worth their own paragraph because they tell the next consumer exactly what to do differently.**

**Bug 1 — Proxy `apply` requires a callable target.** The lazy-construction Proxy at `app/lib/openrouter.ts` was built around `new Proxy({} as OpenRouterProvider, {...})`. When the application code calls `openrouter(MODEL)` (the standard ai-sdk provider invocation pattern), the JavaScript engine looks at the Proxy's TARGET, not the Proxy itself, to decide whether the call should attempt the `apply` trap. If the target isn't a function, the engine throws `TypeError: <name> is not a function` BEFORE the trap can fire. The local dev mode (Turbopack development bundling) inlined the call differently and bypassed the issue — likely because dev mode tree-shakes the lazy path differently or hoists module-load construction. The fix is one line (change the target from `{}` to `() => {}`) but the lesson is broader: lazy-construction Proxy patterns for callable providers MUST use a function target. Added to the [[cloudflare-deploy]] playbook's "Common pitfalls" because this is the kind of thing that costs an hour to track down (the minified bundle showed `yX is not a function` — no obvious source-map link to the openrouter module).

**Bug 2 — Missing ANTHROPIC_API_KEY as a Worker secret.** The walker + seed-detector go direct-to-Anthropic via `@ai-sdk/anthropic` (NOT through OpenRouter) because the long walker system prompt needs Anthropic-style `cache_control` markers that don't cleanly propagate through OpenRouter's transport layer. Easy to forget when wiring secrets — `OPENROUTER_API_KEY` feels like "the" coach key. The playbook now lists BOTH as required runtime secrets, with explanations of which subsystem uses which.

**Security finding worth its own line item** (unchanged from Phase B): Next.js's standalone build bundles values from `.env.local` into the server function bundle by default. When OpenNext consumes that standalone build for a Cloudflare Worker, those values become Worker bundle literals — uploaded to Cloudflare's edge as plain JS strings. Without the `cf-build.ts` wrapper that strips the env AND temporarily moves `.env.local` aside AND post-scans the bundle, a developer running `bun run deploy:cf` locally would silently leak their `OPENROUTER_API_KEY` into the public Worker JS. The 5-second cost of the scan is well worth catching a key-leak before deploy. **Re-verified on the live deploy**: 0 leaks across 11 fetched-from-edge client chunks.

**Adapter choice: `@opennextjs/cloudflare` was the only viable path.** `next-on-pages` is deprecated. Vercel-on-Cloudflare doesn't exist. Hosting on Vercel and proxying through Cloudflare would defeat the "push-to-main rebuilds on Cloudflare" goal. Running raw `wrangler` against a hand-rolled Worker would require reimplementing Next's route handler runtime. OpenNext is the official, supported, current path — and it works.

**Security finding worth its own line item**: Next.js's standalone build bundles values from `.env.local` into the server function bundle by default. When OpenNext consumes that standalone build for a Cloudflare Worker, those values become Worker bundle literals — uploaded to Cloudflare's edge as plain JS strings. Without the `cf-build.ts` wrapper that strips the env AND temporarily moves `.env.local` aside AND post-scans the bundle, a developer running `bun run deploy:cf` locally would silently leak their `OPENROUTER_API_KEY` into the public Worker JS. This finding is captured in the playbook as "the most important pitfall" because it would be invisible without the post-scan step. The 5-second cost of the scan is well worth catching a key-leak before deploy.

**Adapter choice: `@opennextjs/cloudflare` was the only viable path.** `next-on-pages` is deprecated. Vercel-on-Cloudflare doesn't exist. Hosting on Vercel and proxying through Cloudflare would defeat the "push-to-main rebuilds on Cloudflare" goal. Running raw `wrangler` against a hand-rolled Worker would require reimplementing Next's route handler runtime. OpenNext is the official, supported, current path — and it works.

**The 10 MiB bundle limit is a real binding constraint, not theoretical.** Our content tarball is 10.01 MiB compressed — exactly on the line. Without the ASSETS-binding pattern, we'd have been forced to either (a) trim the corpus (would break E-039's exit criterion), (b) move to R2 (would add operational + cost complexity for v1), or (c) pay for Workers Paid (would barely fit and provide no headroom). The ASSETS pattern is the right answer and is documented in the playbook as the default for any app that reads markdown / JSON / image content at runtime.

**Compatibility date matters more than it looks.** `2025-09-01` was chosen because: pre-2025-04-01, `process.env` isn't auto-populated from wrangler `vars` (manual `globalThis.<var>` plumbing needed); pre-2025-09-01, `node:fs` isn't available against the bundled-modules VFS. The cost of bumping to `2025-09-01` is zero (Cloudflare's policy: newer compat dates strictly add capabilities); the cost of choosing `2024-12-30` (OpenNext's example) is several hours of post-deploy debugging of "why isn't this env var visible."

**On the playbook quality**: the playbook documents the pattern, the canonical files, the three biggest gotchas (bundle ceiling, env-bake-in, COOP/COEP through next.config not _headers), and a step-by-step recipe. It would unblock a next consumer (e.g., G-011's web app) without that consumer needing to re-derive any of these decisions. The author's confidence the playbook is correct is "high for the pattern, medium for the live-deploy mechanics" — until Phase C actually runs end-to-end, the recipe steps for the post-deploy `wrangler secret put` are tested only by Cloudflare's own docs, not by us.

## Dead Ends

**`fs.readFileSync` for runtime data files on Cloudflare** — initially considered, since `compatibility_date >= 2025-09-01` gives node:fs against `/bundle`. Doesn't scale: the bundle ceiling cap means 38 MiB of content can't ride along. Replaced with the ASSETS-binding pattern.

**Per-file ASSETS fetch from sync code** — initially attempted to keep `readFileBody()` sync by pre-loading all bodies at boot. Doesn't scale either: 2,379 files × ~16 KiB average = 38 MiB resident in Worker memory per instance, AND the boot-time cost would be hundreds of fetch calls. Replaced with async + lazy + in-memory cache (the cache is hot once a graph-walk starts touching the same 4-7 files repeatedly).

**Letting Next bake env-vars into the bundle and trusting "no public URL means no exposure"** — considered as a "ship now, fix later" path. Rejected. The deploy artifact (wrangler's dry-run output) is the same one that Cloudflare's edge serves; if it has a literal key, that key is on Cloudflare's CDN edge and `curl https://.../foo.js | grep sk-or-` returns it. There is no safe "no one knows the URL" posture for a Worker.

**Storing `OPENROUTER_API_KEY` in a GitHub Actions `env:` block and passing it through wrangler-action** — considered. Rejected. The wrangler-action can set secrets on the Worker on every deploy, but doing it via a GitHub Actions env var means the secret value transits the Actions log on every run. The cleaner pattern (Cloudflare's recommendation) is one-time `wrangler secret put` from a developer machine, with subsequent rotations going through the dashboard.

## Next Steps

The experiment is complete. Follow-ups that didn't land here but are natural extensions:

1. **REQ-001 vibe check.** The live URL is the artifact the user wants for a vibe-check pass against the v5b coach. Ping the user with the URL.
2. **Custom domain** (optional). When/if a memorable hostname is wanted, mint a Zone-scoped Cloudflare token (in addition to the Workers-scoped one), add a `route:` block to `wrangler.jsonc` for the desired host, redeploy. ~30 minutes.
3. **GitHub Actions deploy wiring** (optional / v2). Add `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `OPENROUTER_API_KEY`, and `ANTHROPIC_API_KEY` as GitHub repo secrets (the workflow already references the first three; add ANTHROPIC_API_KEY before turning on automation). Push to main exercises the workflow.
4. **Playbook refinements based on Phase C learnings** — DONE inline: the [[cloudflare-deploy]] playbook now lists the Proxy-target gotcha and the BOTH-secrets requirement under "Common pitfalls."
5. **Latency profile** — Phase C verifications showed ~0.5s for static, ~50s for a full retrieve-and-coach turn on a meaty concern (8 walker steps). Cold-start was 21–25ms per the deploy log. No latency surprises so far; revisit if real users surface complaints.

## Links

- Parent goal: [[G-010]]
- Depends on: [[E-040]] (Next.js scaffold) — the thing being deployed; also [[E-041]] [[E-042]] [[E-043]] [[E-044]] [[E-045]] all need to be deployed-compatible (verified: they are)
- Blocked by: [[REQ-003]] (Cloudflare credentials + GitHub repo secrets)
- Feeds: [[cloudflare-deploy]] playbook (created and linked from the playbooks index)
- Playbook: [[ai-web-app]] (the Next.js + AI SDK + OpenRouter stack being deployed), [[cloudflare-deploy]] (the deploy pattern this experiment authored)
