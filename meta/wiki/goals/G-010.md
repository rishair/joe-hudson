---
type: goal
id: G-010
status: completed
completed: 2026-05-24
created: 2026-05-23
parent: root
---

# Build web app exposing coach chat and wiki

## Outcome

A Next.js web app with two surfaces: (1) a multi-conversation chat interface where users talk to any of the G-009 coach models with full per-message resource attribution, and (2) a navigable wiki view of the `coach/` compendium. The chat and wiki share the same underlying ingested compendium files (one source of truth), so a "resource used" link in a chat message goes to the same wiki entry a user could navigate to manually. The app is locally-first (client-side SQLite for conversation history) with a stateless Next.js backend that owns the system prompt and the OpenRouter API key.

The output is a deployable Next.js app at `web-app/` (or similar) plus an ingestion script that mirrors and link-normalizes `coach/` into the app's local wiki directory.

## Correctness

### Must-haves

- **Stack**: Next.js (App Router) + TypeScript + Vercel AI SDK + OpenRouter as the LLM gateway. Per [[ai-web-app]] playbook.
- **Architecture**: Repository pattern (owns reads + writes for a domain) with constructor-injected composition per [[coding-architecture]] playbook. Zustand is a thin reactive layer over repositories; not a separate "Store" abstraction.
- **Chat surface**:
  - Multiple conversations per user (browser-scoped — no server-side user accounts in v1)
  - Each conversation has many messages; messages persist to **client-side SQLite** (e.g., wa-sqlite + OPFS, or sqlocal) — chosen empirically in R-015
  - Messages are stored at **full AI SDK fidelity**: text, tool calls, tool results, message parts, role, all of it. Symmetry on round-trip: client sends back exactly what AI SDK produced.
  - The **system prompt never reaches the client.** Lives in the Next.js route handler. Client sends messages; server prepends system prompt; server returns streamed response.
  - Chat is wired to the G-009 coach models (initially v5b Sonnet walker; future configs are pluggable). The route handler invokes the same retrieval pipeline as the eval-side coach.
- **Per-message resource attribution**:
  - Each AI message shows the resources (compendium files) used to produce it
  - Display is subtle (does not break conversational flow) — e.g., a small icon or chip count
  - Click → modal with split layout: **left** = full wiki entry for the focused resource, **right** = list of all resources used in that message
  - Clicking another resource in the right panel switches the left panel to that resource's wiki entry
  - "Back to chat" close action returns to the conversation
  - "Expand" action on the wiki entry opens it as a full page in the wiki view
- **Wiki surface**:
  - Navigable view of the ingested `coach/` content — every file is a viewable page
  - Wikilinks within entries (`[[some-file]]`, `[[category/some-file]]`) resolve and are clickable
  - Direct linking via URL slug (e.g., `/wiki/concepts/limiting-belief`)
- **Ingestion script** at `web-app/scripts/ingest-coach.ts` (or similar):
  - Copies `coach/` → `web-app/content/wiki/` (or similar)
  - Normalizes/fixes links so they all resolve in the web app (the `coach/` compendium has inconsistent link formats; the script fixes them)
  - Is **repeatable**: re-running it does not duplicate, does not lose customizations not in `coach/`, and reflects any changes to `coach/` since the last run
  - Is idempotent for the unchanged subset (so the second run with no `coach/` changes is a no-op)
- **Security**: `OPENROUTER_API_KEY` is server-only. No secrets bundled into client JS. System prompts are server-only.
- **No client account system in v1.** Conversations are browser-scoped. If two browsers visit the app, they're separate users.
- **First-visit welcome flow** explaining: (a) what the project is, (b) how it was built, (c) privacy posture (local-only chat storage; LLM provider as only network egress), (d) explicit unaffiliation with Joe Hudson / Art of Accomplishment, (e) brief mention that anonymous feedback may be requested later (no commitment to specific mechanism in v1). Short, dismissible, persists dismissal in localStorage, reachable later via help icon. No dedicated safety step in the welcome — runtime safety handling per [[E-031]] is trusted. See [[E-045]].
- **Deployed to Cloudflare and accessible at a public URL** with an automated GitHub→Cloudflare pipeline (push to main triggers rebuild + redeploy). `OPENROUTER_API_KEY` is configured as a Cloudflare secret, never in the repo or client bundle. Added 2026-05-24 per user direction — the app being "buildable locally" was insufficient; it needs to actually be shareable. See [[E-046]]. The pattern that emerges from E-046 becomes a `cloudflare-deploy` playbook for future projects.

### Nice-to-haves

- Per-message cost display
- Export/import of a conversation
- Conversation sharing (URL with conversation state encoded)
- Anonymous feedback mechanism (e.g., 👍/👎 per assistant message + optional one-line comment, no account, no PII). Deferred from v1 per user direction on 2026-05-24; the welcome flow mentions it as a future possibility so the framing is in place for a clean future addition.
- Wiki search (full-text or keyword)
- Mobile-responsive design
- Streaming tool-call rendering (show the tool call appearing in the message in real time)
- Multiple coach configs selectable per conversation (e.g., "try this conversation with the Haiku walker instead")
- User account system (deferred to G-011+)

## Exit Criteria

- [x] Next.js app runs locally with `cd web-app && bun run dev` (E-040 baseline; dev server has been continuously serving through E-041/E-042/E-045 work)
- [x] Ingestion script `web-app/scripts/ingest-coach.ts` produces `web-app/content/wiki/` from `coach/` with all internal wikilinks resolvable (98.63% body / 98.46% related resolve; remaining breakages are real corpus gaps recorded in manifest for G-007) — E-039
- [x] Re-running the ingestion script after a `coach/` change reflects the change; re-running with no changes is a no-op (manifest-driven hash-skip; verified end-to-end) — E-039
- [x] Chat UI starts a new conversation and supports at least 10 turns end-to-end with a v5b-equivalent coach config (E-043 — 10-turn smoke ran successfully against `/api/chat`; each turn produced a short, in-voice, single-question coach reply with 2-6 retrieved resources; total cost ~$1.74 for the 10-turn run; multi-turn context preserved through turn 8's "you started going somewhere just now" callback)
- [x] Messages persist to client-side SQLite; refreshing the page reloads the conversation in full fidelity (tool calls intact) — E-041 (byte-equality round-trip verified across 5 part shapes including tool-call+tool-result)
- [x] Server route handler invokes OpenRouter; no `OPENROUTER_API_KEY` is visible in network requests or client bundle (E-040 baseline; re-verified during E-041 across 21 dev-mode client JS chunks)
- [x] System prompt is not visible in any network request from the client (E-040 baseline; `'server-only'` import on `lib/system-prompt.ts` enforces; re-verified during E-041)
- [x] Each AI message displays a subtle resource attribution; clicking it opens the split modal (wiki entry left, resource list right) — E-044 (resource modal — native `<dialog>` + nuqs URL state, `<WikiRenderer linkBehavior="in-modal">` for left panel, expand→full-wiki w/ back-to-chat)
- [x] Wikilinks within a wiki entry resolve to the correct local page (E-042 — 98.63% body / 98.46% related resolve rates land in the rendered DOM as Next.js `<Link>` for resolved targets and `<span class="wikilink-broken">` for unresolved ones; piped display + anchors preserved)
- [x] At least one cost-cap or rate-limit mechanism on the route handler so a runaway client cannot drain the OpenRouter budget (E-043 — process-level cumulative-spend circuit breaker at `app/lib/coach/cost-cap.ts`; `OPENROUTER_DAILY_CAP` env var, default $5; HTTP 503 with descriptive body on exceed; verified with `OPENROUTER_DAILY_CAP=0.0001` — first call burned $0.0072, second call returned 503)
- [x] First-visit welcome flow shows project intro, build summary, privacy posture, unaffiliation language; dismisses to localStorage; re-openable via help icon — E-045
- [x] App is deployed to Cloudflare at a public URL; all 6 features (welcome, chat, wiki, persistence, modal, cost-cap) work in the deployed environment; no `OPENROUTER_API_KEY` in any client payload — E-046 (live at https://joe-hudson-coach.rishi-ishairzay.workers.dev; deployed 2026-05-24; 0 secret leaks in live client chunks; chat retrieve+coach pipeline verified end-to-end with a real concern prompt). GitHub-Actions automation (push-to-main → rebuild) intentionally deferred to v2 per user direction — workflow file is shipped and correct, repo secrets not yet provisioned, current deploys run from developer machine via `bunx wrangler deploy`.

## Context

G-009 produced the v5b coach (Sonnet walker over graph-walk retrieval, overall 2.71 on the G-008 eval). G-010 makes it usable — gives Rishi a way to actually talk to the coach interactively, and surfaces the compendium that powers it. This is the consumer of all prior work.

User-supplied constraints from the original transcript:
- Stack: Next.js + TS + AI SDK + OpenRouter (mandatory, captured in [[ai-web-app]] playbook)
- Client-side SQLite for chat history (full-fidelity AI SDK messages)
- System prompts server-only
- Single ingested copy of `coach/` shared by chat tools and wiki UI
- Subtle per-message resource attribution; modal-based deep dive
- Architecture: Repository pattern with composition; Zustand for reactive UI integration (captured in [[coding-architecture]] playbook; user revised on 2026-05-23 to drop the separate Store concept)

## Resources

- `OPENROUTER_API_KEY` (to be provided by user — see Resource Requests below) — server-only env var
- `ANTHROPIC_API_KEY` in `.env` is available but the playbook prefers OpenRouter routing for this app's stack
- `coach/` directory — source compendium (read-only from G-010's perspective; G-007 owns mutations)
- The G-009 retrieval pipeline lives at `coach-app/retrieval/` — the web app should adapt or reuse it (decision deferred to R-016)
- Playbooks: [[ai-web-app]], [[coding-architecture]]
- Estimated budget: TBD by user. Build cost is mostly local development (free); runtime/eval cost depends on use volume.

### Resource Requests

- [[REQ-002]] OpenRouter API key needed before any LLM-calling experiment can run — **resolved** 2026-05-24
- [[REQ-003]] Cloudflare account + API token + GitHub repo secrets needed before E-046 live deploy — **pending**

## Sub-goals

(none yet)

## Research

- [[R-015]] What's the best browser-side SQLite approach for full-fidelity AI SDK message persistence? — **complete** (recommendation: `sqlocal` + OPFS; schema: 2 tables, JSON column for `UIMessage`; COOP/COEP from day one)
- [[R-016]] How should the G-009 retrieval pipeline (graph-walk + Sonnet walker) be adapted from `coach-app/` to the web app? — **complete** (strategy: lift to `web-app/app/lib/coach/`; use `@openrouter/ai-sdk-provider` for cache_control; AI SDK v5 custom `data-resources` parts for telemetry)
- [[R-017]] What's the canonical pattern for resource-attribution UX in chat (subtle in-flow indicator + modal deep-dive)? — **complete** (subtle bottom-of-message strip → native `<dialog>` modal, 65/35 split; URL state via `nuqs` NOT parallel/intercepting routes; `linkBehavior="in-modal"` on E-042 renderer for left-panel wikilinks)
- [[R-018]] What link conventions exist in the current `coach/` directory and what normalization is needed? — **complete** (canonical form: `[[slug]]` / `[[slug|display]]` with optional `#anchor`; 6 normalization rules specified; case-fold resolution auto-fixes 22 of 491 broken body links; corpus is 2,376 files with 0 slug collisions and 98.6% body / 98.5% related link resolve rate; E-039 unblocked)

## Experiments

**Phase 1 — Foundation:**
- [[E-039]] Build repeatable ingestion script: copy `coach/` → web app content dir, normalize links — **succeeded** (2026-05-24)
- [[E-040]] Scaffold Next.js + AI SDK + OpenRouter minimal chat (no persistence, no wiki, no resource UI) — **succeeded** (2026-05-24)

**Phase 2 — Persistence & wiki:**
- [[E-041]] Add client-side SQLite for full-fidelity conversation persistence — **succeeded** (2026-05-24)
- [[E-042]] Build wiki view: routing, rendering, internal wikilinks — **succeeded** (2026-05-24)

**Phase 3 — Integration:**
- [[E-043]] Wire the v5b coach (graph-walk retrieval + Sonnet walker) into the chat route handler — **succeeded** (2026-05-24)
- [[E-044]] Build resource-attribution modal: split layout, navigate between resources, back-to-chat, expand-to-full — **succeeded** (2026-05-24; native `<dialog>` + nuqs, reused E-042's `<WikiRenderer>`, consumes E-043's `data-resources` AI SDK part)

**Phase 4 — Onboarding:**
- [[E-045]] Build first-visit welcome flow (project intro, build summary, privacy, unaffiliation) — **succeeded** (2026-05-24)

**Phase 5 — Deployment:**
- [[E-046]] Deploy web-app to Cloudflare with automated GitHub pipeline — **succeeded** (2026-05-24; live at https://joe-hudson-coach.rishi-ishairzay.workers.dev; GitHub Actions automation deferred to v2 per user)

**Backlog (under this goal, not currently planned for v1):**
- [[B-001]] Corpus directory/index structure experiments
- [[B-002]] In-line lesson surfacing during a user session
- See [[backlog/index.md]] for the full auto-generated backlog index across all goals

## Decision Log

- 2026-05-23: Goal created. Successor to G-009 (coach build). Makes the coach actually usable. Stack and architecture decided by user via questionnaire (App Router, OpenRouter always, interface+class repositories, Zustand). Two playbooks created to capture stack + architecture defaults: [[ai-web-app]] and [[coding-architecture]]. Goal is broken into 4 research items (browser SQLite, retrieval-pipeline adaptation, resource-attribution UX, link conventions) and 6 experiments across 3 phases (foundation → persistence/wiki → integration).
- 2026-05-23: **Coding-architecture playbook revised: dropped the separate "Store" concept.** User direction: Repository handles both reads and writes for a domain; no separate Store primitive. Composition pattern still applies. Zustand reframed in the playbook as a thin reactive layer over repositories rather than a Store abstraction. G-010 must-haves and experiment specs updated to remove Store references. Reason: the read/write split caused churn whenever something straddled the boundary; one Repository per domain is cleaner.
- 2026-05-23: **Coding-architecture playbook revised again: prefer few methods with rich param objects over many narrow methods.** Repository surface is now the four-method shape: `find(criteria)`, `create(input)`, `update(criteria, patch)`, `delete(criteria)`. The narrow alternative (`getById`, `getByEmail`, `updateName`, `deleteByOrgId`, etc.) is explicitly an anti-pattern. Reason: smaller surface per class, wrappers are dramatically simpler (4 methods to wrap, not 12+), adding a new filter dimension is an options-bag addition rather than a new method. E-041's ConversationRepository and MessageRepository specs updated. E-042's WikiRepository spec updated. Playbook example code rewritten to show the unified pattern + cache/log wrappers using it.
- 2026-05-23: **REQ-002 filed**: OpenRouter API key needed. G-010 cannot run any LLM-calling experiment without it. Existing `ANTHROPIC_API_KEY` could be a fallback if OpenRouter integration is blocked, but the playbook makes OpenRouter mandatory for this stack — going direct-to-Anthropic would diverge from the playbook and complicate later model swaps.
- 2026-05-23: **Local-first, no server accounts in v1.** Conversations live in browser-side SQLite per the user transcript. This makes deployment trivial (stateless backend), defers the auth question entirely, and lets the user demo by sending a URL. Trade-off: no cross-device sync. Acceptable for v1; future goal can layer in optional server-side sync.
- 2026-05-23: **Full-fidelity message persistence is non-negotiable per the transcript.** Storing only text would break tool-call round-tripping. The AI SDK's `UIMessage` type is the persistence unit — store the entire structured message, not a flattened text representation.
- 2026-05-23: **Single ingested copy of `coach/`** rather than two parallel reads. The chat's tools and the wiki's routes both read from `web-app/content/wiki/`. Ingestion script is the only place that touches `coach/`. This keeps tools and wiki guaranteed-consistent and makes link-normalization a one-time-per-ingest cost.
- 2026-05-23: **Audit performed** ([[G-010-audit]]). Critical findings: (1) E-043 is dramatically under-specified — the eval-side glue between retrieval and the coach call (`eval/lib/conversation.ts:270-352`, `eval/lib/retrieval-adapter.ts:122-181`) is NOT present in `coach-app/run.ts` and must be replicated by E-043; OpenRouter cache semantics for the Anthropic `cacheControl` markers used in `coach-app/lib/anthropic-client.ts` are unaddressed. (2) E-041 is partially-revised: interfaces follow the new four-method shape but hypothesis, step 6, and blast-radius still reference the old narrow methods and the dropped "Repository/Store" split. (3) E-039's corpus-size estimate of ~470 files is wrong by ~4x — actual is ~1,856 markdown files in the 14 standard categories (R-014 reported ~1,059 mid-month; G-007 absorption continues). (4) Leftover architectural-Store references in R-015, E-040, E-041 contradict the playbook revision. Important: no equivalence test that web-app v5b ≈ eval-measured v5b; full-fidelity persistence success criterion is too soft to catch silent drops; cost-cap mechanism is hand-waved given no-auth v1; R-016 doesn't mandate an answer on AI SDK message-annotation mechanism for telemetry transport; E-044 references partial routing-file syntax as if R-017 has decided. Recommendations docket queued for `wiki-next`.
- 2026-05-23: **Audit resolved.** All 4 critical findings + most important findings addressed. Key changes: (1) E-043's Hypothesis + Method rewritten to reflect that the retrieval-to-coach orchestration lives in the EVAL harness (`eval/lib/conversation.ts`, `eval/lib/retrieval-adapter.ts`), NOT in `coach-app/run.ts` — porting the v5b coach is not a small adapter job. Method now separates "port" (walker, graph, safety) from "new orchestration code" (trigger policy, injection, history, retry, cost-cap). (2) R-016 restructured to enumerate four MUST-answers before E-043 can start: orchestration port location, prompt-cache markers under OpenRouter (`providerOptions.anthropic.cacheControl` won't propagate through `@ai-sdk/openai`), AI SDK message-annotation transport for telemetry, adaptation strategy comparison. (3) E-043 adds a behavioral verification step: re-run G-008 eval against the web-app route handler as coach target; mean per-dim must land within ±0.1 of E-038's 2.71. (4) E-043 cost cap concretised as process-level circuit-breaker (`OPENROUTER_DAILY_CAP=5` USD default, 503 on exceed) — per-IP/per-user limits are auth-gated and v2. (5) E-039 corpus count corrected (1860 not 470), integrity threshold revised to 90%, ingestion coordination with `rebuild-index.sh` flagged, scope clarified that output dir is mirror-only. (6) E-041 Repository/Store leftovers and old narrow-method references cleaned up; success criterion tightened to byte-equality of UIMessage JSON round-trip. (7) R-018 explicitly handles Obsidian alias syntax `[[slug|display]]`. (8) `ai-web-app.md` useChat import path corrected from `ai/react` to `@ai-sdk/react`. Audit status moved to `addressed`; see `meta/wiki/audits/G-010-audit.md` Resolution section for the per-finding accounting.
- 2026-05-24: **OpenRouter key landed; REQ-002 resolved.** User provided `OPENROUTER_API_KEY` and `HF_TOKEN`; both stashed in `.env` (gitignored). E-040 and E-043 are unblocked on credentials. R-016 (the four MUST-answers) remains the next research priority before E-043 can actually run.
- 2026-05-24: **Welcome flow added as E-045 (Phase 4).** User direction via the goal command: short and sweet first-visit modal explaining what the project is, roughly how it was built, privacy (local-only, LLM provider is only egress), and explicit unaffiliation with Joe Hudson / Art of Accomplishment. Decisions via questionnaire: (a) unaffiliated framing explicit, (b) NO dedicated safety step in welcome (trust runtime safety per E-031), (c) anonymous feedback mechanism DEFERRED — welcome mentions it may be requested later but commits to no specific UI in v1; added as nice-to-have for a future experiment, (d) first-visit-only, dismissible to localStorage, re-openable via help icon. Welcome content spec'd in E-045 across 4 sections (what, how, privacy, get started) with example seed prompts that mirror REQ-001's vibe-check suggestions.
- 2026-05-24: **R-015 complete: browser-SQLite choice is `sqlocal` (wrapping official `@sqlite.org/sqlite-wasm`) backed by OPFS.** Key downstream implications: (a) `next.config.js` must set `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp` from day one — this is safe because OpenRouter calls run server-side from `/api/chat`, so COEP doesn't block them; (b) schema is two tables (`conversations`, `messages`) with a single JSON column for the serialized `UIMessage` — NOT a normalized schema, because normalization breaks the byte-equality round-trip the audit tightened E-041's success criterion to require; (c) no Drizzle/Kysely for v1 (two tables, three queries — overkill); (d) `opfs-sahpool` is the no-headers fallback (single-connection-per-origin tradeoff) accessible via a one-line VFS flip if COOP/COEP turns out to break some embed later; (e) AI SDK 5+ `UIMessage` is the persistence unit (not `ModelMessage`, which is lossy); whatever metadata transport R-016 picks for retrieval telemetry, it lives inside the `UIMessage` and is captured automatically by the JSON column with no schema change. E-041 unblocked on R-015; remaining blockers are E-040 (chat scaffold) which is itself unblocked. E-041's hypothesis/method updated to name `sqlocal` concretely instead of "the R-015 stack."
- 2026-05-24: **R-016 complete: all four MUST-answers resolved.** (1) Orchestration port — lift three files from coach-app (`retrieval/guided-walk.ts`, `retrieval/graph-walk.ts`, `lib/safety.ts`) into `web-app/app/lib/coach/`; rewrite the eval orchestration (`eval/lib/conversation.ts:270-368`) inline in the `/api/chat` route handler (trigger gate → retrieve → inject-into-user-message → preserve bare-client-message in history → graceful-degrade on retrieval failure); leave `coach-app/run.ts`, embeddings, pre-screen, and the multi-strategy switch behind. (2) Prompt-cache markers — use `@openrouter/ai-sdk-provider`'s `createOpenRouter` (NOT `@ai-sdk/openai` with a custom baseURL); set `providerOptions.openrouter.cacheControl: { type: 'ephemeral' }` on the system message. Preserves the playbook's "one gateway" rule AND ~10x prompt-cost savings (~$0.001 vs ~$0.01 per turn for v1's ~3.1K-token prompt). Caveat: known issue OpenRouterTeam/ai-sdk-provider#35 means cache_read/cache_write tokens may not always surface on usage; verify during E-043. (3) Telemetry transport — AI SDK v5+ custom typed data parts via `createUIMessageStream` (NOT removed `streamData`/`writeMessageAnnotation`, NOT shimmed-in tool calls). Define `CoachUIMessage = UIMessage<never, { resources: ResourceAttribution }>`, write `data-resources` part BEFORE `streamText` begins. Part lives in `message.parts` so E-041's byte-equality round-trip handles it automatically with no schema change. (4) Adaptation strategy — pick **(b) lift to `web-app/app/lib/coach/`**. (a) reuse-as-is fails because coach-app is Bun-only (`import.meta.dir`, `.ts` extension imports) and Next.js runs on Node. (c) extract-to-shared-package is over-engineered: it would be the codebase's first workspace package for marginal coupling gain. (b) costs ~30 min of file copies + path-constant edits + Node-compat tweaks; drift risk is low because G-009 is feature-frozen. Required playbook patch (separate small work item): `ai-web-app.md` should swap `createOpenAI({ baseURL })` for `createOpenRouter` and document that the OpenAI compat shim does NOT preserve Anthropic provider options like cache_control. E-043 unblocked. E-041 (no change beyond using `CoachUIMessage` as the type parameter) and E-044 (consumes `message.parts.find(p => p.type === 'data-resources')`) also unblocked from a contract perspective. Embeddings model (`bge-small-en-v1.5`) confirmed NOT needed for v1 (graph-walk uses graph traversal only).
- 2026-05-24: **R-018 complete: link conventions characterized; normalization spec finalized.** Surprising findings vs prior expectations: (1) Slugs are globally unique across all 13 categories (0 collisions), so `[[bare-slug]]` is always unambiguous and the renderer needs only a `Map<slug, {category, title}>` for resolution. (2) The corpus is much healthier than R-014 predicted — 98.6% of body wikilinks and 98.5% of `related:` entries resolve cleanly out of 34,154 body links and 15,040 `related:` targets. (3) Format diversity is tiny: 99.24% of body links are bare lowercase slugs, 0.60% are piped (`[[slug|display]]`), and the remaining 0.16% are edge cases (uppercase, category-prefixed, spaces, anchors). (4) `related:` arrays are 100% inline JSON-style with no YAML block-list variants. (5) Only 1 markdown-syntax internal link (`[text](path.md)`) in the entire corpus — Obsidian wikilinks are effectively the only convention. (6) Auto-fixable: 22 case-mismatch body links (e.g., `[[VIEW]]` → `view`, `[[Why-questions]]` → `why-questions`) become valid via case-insensitive resolution. Canonical web-app URL form: `/wiki/{category}/{slug}{#anchor?}`; canonical source-link form: `[[slug]]` or `[[slug|display]]`. E-039 unblocked with 6 explicit normalization rules. E-042's renderer needs a single canonical parser plus a slug→category map. E-039 updated: corpus count corrected to 2,376; integrity threshold raised from 90% to 97%; success criteria now include auto-fixing 22+ case-mismatches, manifest shape matches R-018 spec, and fail-loud on duplicate slug as forward-compat. Also filed G-007 docket items: bulk-fix 22 case-mismatch source links; triage 469 missing-target wikilinks with `the-pucker-as-the-truth-marker` (5 refs), `dont-use-view-as-technique` (4), `fix-the-team-by-fixing-the-leader` (4), `loving-the-emotion` (3) as the highest-leverage targets to author.
- 2026-05-24: **R-017 complete: resource-attribution UX pattern decided.** Subtle bottom-of-message strip ("N sources") on each assistant message → click opens a native HTML `<dialog>` modal (65/35 split layout, focused resource in left panel, full resource list in right panel). Modal state is URL-encoded via the `nuqs` library (`?resourceModal=<msgId>&focus=<slug>`), explicitly NOT via Next.js parallel + intercepting routes despite the partial-syntax leftover in E-044. Rationale: the parallel-routes-for-modals pattern causes modal-stacking on in-modal navigation (Next.js issue #71586) and adds a fragile `[...catchAll]/page.tsx` slot maintenance burden. Our UX explicitly requires intra-modal focus changes (clicking a right-panel item, clicking a wikilink in the left panel) which are not route navigations and should not touch the router. nuqs `useQueryState` with `history: 'push'` gives us shareable URLs + browser-back-closes-modal without route files. Per-message resources are read directly from R-016's `data-resources` custom typed part on the assistant `UIMessage` (`message.parts.find(p => p.type === 'data-resources')`) — no separate state store. The planned Zustand `useResourceModalState` collapses to a `useResourceModal()` hook over nuqs + the existing chat messages array. "Expand to full" pushes `/wiki/<slug>?fromChat=<msgId>` and the E-042 wiki page reads `?fromChat=` to render a "Back to chat at message N" link (`router.back()` primary, `router.push('/?scrollToMessage=' + msgId)` fallback). Wikilinks inside the modal must NOT navigate — E-042's wiki renderer needs a new `linkBehavior: 'route' | 'in-modal'` prop (default `'route'`), and E-044 passes `'in-modal'` with an `onLinkClick(slug)` callback wired to `focus(slug)`. Modal shell is the native `<dialog>` + `showModal()` pattern (focus-trap, escape, backdrop, ARIA — all built in); no Radix dependency for v1. Layout: 65/35 split, ~90vw × 85vh, max 1400px wide. E-044 blast radius simplified: drop `app/@modal/(.)/wiki/...`, add `nuqs` dep, single `app/components/resource-modal.tsx` + `app/lib/state/use-resource-modal.ts`. E-042 must add a `linkBehavior` prop and an `onLinkClick` callback to its WikiRenderer + read `?fromChat=` to surface the back-to-chat affordance.
- 2026-05-24: **E-039 succeeded.** `web-app/scripts/ingest-coach.ts` (Bun/TypeScript, ~600 LOC) mirrors `coach/` → `web-app/content/wiki/` implementing R-018's 6 normalization rules. Full-corpus run: 2,376 files in 0.85s cold / 0.69s warm (~70× under the 60s target). Integrity: 98.63% body / 98.46% related resolve rate, both above the 97% threshold by ≈1.5pp. Idempotency verified end-to-end: re-run with no source changes = 0 writes; touch source = 0 writes (hash-skip); modify source = exactly 1 write; remove source = output deleted. Duplicate-slug guard fails loudly with non-zero exit and descriptive error. Manifest at `web-app/content/wiki/.ingest-manifest.json` matches R-018's spec exactly (version, files, broken_links, broken_related, slug_to_category). Derived files (`_index.md` 2,376 entries, `_backlinks.json` 2,599 targets) regenerated from normalized output per Rule 6 — chose to inline the algorithm in TypeScript rather than parameterize `coach/bin/rebuild-index.sh` so the ingestion script stays self-contained (no Python dependency, no shell-out, ~50 LOC added). Case-fold normalization (Rule 1.4) verified to auto-fix `[[VIEW]]` → `[[view]]` and `[[Why-questions]]` → `[[why-questions]]` in the 3 source files that had them. The 1 markdown-syntax internal link in the corpus (`[comedian-to-coach session](./../examples/comedian-to-coach-money-shame-session.md)` in `moves/ask-whats-wrong-with-you-into-the-shame.md`) was rewritten to `[[comedian-to-coach-money-shame-session|comedian-to-coach session]]` per Rule 3. The 466 body-link breakages and 231 related breakages that remain are real corpus gaps and are recorded in the manifest for G-007's docket (already enumerated in R-018). TypeScript typecheck clean. `bun run ingest` script alias added to `web-app/package.json`. Frontmatter rebuild uses a pass-through-with-selective-rewrite strategy: only `related:` lines are rewritten, other lines pass verbatim — this avoids subtle whitespace diffs and keeps the idempotency hash stable across re-runs. Integrity threshold enforcement is suppressed for subset runs (`--limit N` / `--only CATEGORY`) since cross-category targets are excluded by construction. **Unblocks E-042** (renderer reads `slug_to_category` from the manifest) **and E-043** (chat tools read from `web-app/content/wiki/`). Phase 1 of G-010 complete (E-039 + E-040 both succeeded).
- 2026-05-24: **E-040 succeeded.** Minimal Next.js + AI SDK + OpenRouter chat scaffold stood up at `web-app/`. Stack: `next@16.2.6` + `react@19.2.6` + `ai@6.0.191` + `@ai-sdk/react@3.0.193` + `@openrouter/ai-sdk-provider@2.9.0` + `zustand@5.0.13`. Production build clean (4 routes, 9.5s compile). Multi-turn streaming chat verified end-to-end against `anthropic/claude-sonnet-4.6` via OpenRouter (3-turn name/spell-backward/letter-count test preserved context). Zero leakage of `OPENROUTER_API_KEY` or system-prompt text across 51 dev-mode JS chunks AND 10 production-build JS files — the `'server-only'` import on `lib/openrouter.ts` and `lib/system-prompt.ts` is the protection mechanism. COOP/COEP set in `next.config.js` from day one so E-041's `sqlocal` + OPFS inherits a working environment without later header surgery. Two follow-up playbook patches surfaced (not standalone wiki work items — small inline edits when next someone touches the playbook): [[ai-web-app]] should recommend `@openrouter/ai-sdk-provider` instead of `@ai-sdk/openai` with a custom baseURL (R-016's decision; the OAI shim drops the cache_control E-043 needs), and should use the dot form for OpenRouter model IDs (`anthropic/claude-sonnet-4.6`, not `claude-sonnet-4-6` which 404s on OpenRouter). Per audit minor #13, kept the scaffold truly minimal — no `lib/repos/`, no `lib/state/`, no `lib/types/`; those land in E-041 when persistence forces them. Unblocks: **E-041** (fully unblocked, R-015 already done), **E-045** (welcome flow, single dep was E-040). E-042 and E-043 still blocked on E-039 (ingestion).
- 2026-05-24: **E-045 succeeded.** First-visit welcome flow landed at `web-app/app/components/welcome.tsx` + `help-icon.tsx`, mounted in `app/layout.tsx`. Pattern: native HTML `<dialog>` via `.showModal()` / `.close()` plus a `localStorage.welcomeAcknowledged` flag. HelpIcon dispatches a `window` event the Welcome subscribes to — keeps the two components as independent siblings of `layout.tsx` with no shared store. 4 sections per spec (What this is / How it was built / Privacy / Get started) with the unaffiliation language italicized under §1 and the local-SQLite + LLM-provider-as-only-egress language explicit in §3. Returning users render `<></>` until the localStorage read completes, preventing a hydration flash of the modal. Verifications: `bun run typecheck` clean, `bun run build` clean (4 routes, 8.1s), SSR HTML contains the help icon with correct `aria-label` and the welcome content is bundled in the client chunk (`grep welcomeAcknowledged|Art of Accomplishment|local SQLite|OpenRouter|LLM provider|anonymous feedback|stuck on|...` all hit), zero `fetch(`/`XMLHttpRequest`/`sendBeacon`/analytics references in the welcome chunk (privacy guarantee preserved), `/api/chat` regression test (1-turn streaming) returns 200 with the full SSE sequence intact. **Choices vs spec:** (a) used vanilla CSS + inline styles instead of the spec-mentioned Tailwind (E-040 ships no Tailwind; staying consistent rather than introducing the dep for one component), (b) used native `<dialog>` rather than a portal+focus-trap+escape-handler stack — gets the platform's focus trap / escape / backdrop / ARIA-modal-role free and validates the pattern E-044 will reuse for the resource modal at larger scale, (c) button label switches "I got it →" (first visit) to "Close" (help-icon reopens), (d) window-event channel between HelpIcon and Welcome instead of Zustand — trivial to lift to Zustand later if multiple consumers appear. **Not done (intentional):** no browser-driven test (no Chrome/jsdom in repo; SSR HTML + chunk grep + regression test on chat carry the load-bearing verification); no copy editing pass (used spec's literal copy). **Phase 4 of G-010 complete.** Welcome-flow exit criterion ticked. Remaining exit criteria: chat 10-turn (E-043), SQLite persistence (E-041), resource attribution (E-044), wikilink resolution (E-042), cost-cap (E-043). HelpIcon should migrate into a top-bar if E-042 introduces one.
- 2026-05-24: **E-042 succeeded.** Wiki view runs at `/wiki/...`. New code: `app/wiki/[[...slug]]/page.tsx` (optional catch-all), `app/components/wiki-renderer.tsx` (react-markdown + remark-gfm + custom `a` dispatching by URL shape and `linkBehavior` prop), `app/components/back-to-chat.tsx`, `app/components/chat-scroll-restorer.tsx`, `app/lib/wiki/*` (FsWikiRepository, types, frontmatter parser, wikilink processor, container). One small modification to `app/page.tsx`: `<Suspense><ChatScrollRestorer/></Suspense>` mount, `id="msg-{id}"` on rendered message articles, sidebar Wiki link. `react-markdown@^10` and `remark-gfm@^4` added. **Architecture decisions:** (a) wikilink pre-processing runs server-side in the route, keeping the slug index (~2,376 entries) out of the client bundle; (b) broken links use a sentinel `_broken` category so the renderer's `<a>` component dispatches by URL shape with no extra prop; (c) inline styles to match E-040/E-041/E-045 instead of the spec-mentioned Tailwind; (d) the `?fromChat=` reader and the chat-page `?scrollToMessage=` restorer are tiny separate client islands in `<Suspense>` so the wiki page can stay a server component. **Verifications:** typecheck clean, prod build clean (now 4 routes: `/`, `/_not-found`, `/api/chat`, `/wiki/[[...slug]]`), dev render times 166–416ms on `/wiki/concepts/limiting-belief` (well under the 500ms target), 54 wikilinks resolve correctly on `limiting-belief`, 7 broken-link spans + 3 resolved anchors on `moves/use-the-strong-word-for-somatic-landing` (a known-broken-heavy page), anchor preserved (`fear-of-joy#impermanence-is-what-makes-the-thing-sweet`), piped display works (`[[fear-triangle|victim role]]` → `victim role` text on a `/wiki/frameworks/fear-triangle` href), `?fromChat=` renders the back-link only when set, `/wiki/_broken/limiting-belieff` (typo) lands on a polite "Did you mean Limiting Belief?" page via bounded-Levenshtein over the slug index. **Race with E-041:** E-041 landed `app/page.tsx` mid-experiment; I re-read after the file-modified guard fired and merged my 3 surgical edits (suspense mount, id attribute, sidebar link) into the new E-041 structure without conflicting with the conversation/messages persistence work. **Exit criterion ticked:** "Wikilinks within a wiki entry resolve to the correct local page." **Unblocks E-044** (resource modal imports `<WikiRenderer linkBehavior="in-modal" onLinkClick={focus} />` and consumes the existing `?fromChat=`/`?scrollToMessage=` pipe for "Expand to full" + back-to-chat). Operator note: the wiki manifest cache is keyed off process lifetime; re-running `bun run ingest` while `next dev` is running requires restarting the dev server for new pages to appear.
- 2026-05-24: **E-041 succeeded.** Client-side SQLite persistence landed at `web-app/`. Stack: `sqlocal@0.18.0` (wrapping `@sqlite.org/sqlite-wasm`) with OPFS backend; data-migrations playbook's class-based runner with `navigator.locks` serialization; two `Sqlite*Repository` classes behind the playbook's four-method interfaces (`find`/`create`/`update`/`delete` with rich criteria objects); thin Zustand reactive layer (`useConversationState`) that bootstraps from SQLite on mount and mirrors writes back. **Behavioral verification** (`bun run verify:e041`) covers 18 checks across 6 buckets: migration idempotence, conversation CRUD, byte-equality round-trip of `UIMessage` JSON across 5 representative part shapes (text, reasoning+text, static tool call + result + metadata, dynamic tool + R-016 `data-resources` part + text, user with file part), 10-way concurrent insert producing unique sequential positions, FK cascade on delete, single-message delete. All 18 pass. **Three implementation decisions documented in E-041's Analysis:** (1) `PRAGMA foreign_keys = ON` is required per-connection — SQLite default-disables FK enforcement and the first behavioral test caught the missing cascade as "10 stragglers"; pragma added to `getDb()` after the warmup `SELECT 1`. (2) ConversationRepository and MessageRepository stayed split per audit issue #2(e) consideration — collapsing would either fetch messages eagerly on every conversation list (wasteful) or force a narrow `findMessages` second method (anti-pattern); two repositories sharing one SQLocal instance at the wiring layer captures the cohesion correctly. (3) Message position is assigned atomically inside a transaction (`SELECT MAX(position)+1` + `INSERT`) so concurrent `messages.create` calls within the same conversation cannot collide. **E-040 chat path unchanged:** end-to-end POST `/api/chat` still streams from OpenRouter (`anthropic/claude-sonnet-4.6`) and zero leakage of `OPENROUTER_API_KEY` or system-prompt text in any of the 21 client JS chunks. **Race with E-042:** E-042's edits to my new `app/page.tsx` (sidebar Wiki link, ChatScrollRestorer suspense mount, message id attributes) integrated cleanly because the surface area my code touched (sidebar header, message rendering, useChat wiring) is orthogonal to theirs. The notification arrived mid-implementation; re-read, merge was three-way clean. **Exit criterion ticked:** "Messages persist to client-side SQLite; refreshing the page reloads the conversation in full fidelity (tool calls intact)" — the byte-equality test on sample[2] (assistant with tool call + tool result + metadata) is exactly the load-bearing assertion that exit criterion makes. **Unblocks E-044** entirely on the persistence side (modal reads `message.parts.find(p => p.type === 'data-resources')` from the rehydrated UIMessage which round-trips byte-exact through SQLite). **E-043 dependency satisfied** for the persistence-of-orchestration-output path. Two remaining exit criteria depend on E-043 (chat 10-turn with v5b coach, cost-cap) and one on E-044 (resource attribution modal). Test reproducibility: `cd web-app && bun run verify:e041` runs the full 18-check suite in <1s using sqlocal's in-process memory driver, since headless Chrome is unavailable here; the in-memory path differs from OPFS only in the worker-bootstrap hop and file persistence — both sqlocal-internal — while the Repository code, migrations, byte-equality serialization, and FK cascade are identical across modes.
- 2026-05-24: **E-043 succeeded.** v5b coach (graph-walk + Sonnet walker + v1 prompt + runtime safety) wired into `/api/chat`. New `web-app/app/lib/coach/` namespace lifts three modules from `coach-app/` (`graph-walk.ts`, `guided-walk.ts`, `safety.ts`) with Node-compat adaptations (`WIKI_ROOT` env-var, no `import.meta.dir`, extensionless imports), adds a thin `retriever.ts` (replaces `eval/lib/retrieval-adapter.ts`'s multi-strategy switch with V5B_CONFIG hardcoded), a process-level cost-cap circuit breaker (`OPENROUTER_DAILY_CAP=5` default → HTTP 503 on exceed), and `types.ts` with `CoachUIMessage` + `ResourceAttribution`. `system-prompt.ts` placeholder replaced with v1 prompt as a literal (byte-identical to source, verified by length+phrase-count comparison). `/api/chat` route fully rewritten with cost-cap pre-check → safety pre-scan → retrieval call with graceful degradation → augmented-user-message build (safety reminder + injection + bare user text, only on the model side — persisted history stays bare) → HAPPY PATH `createUIMessageStream` emitting `data-resources` BEFORE `streamText` merge / SAFETY PATH non-streaming generate → post-check → regenerate with stronger reminder → `SAFETY_FALLBACK_TEMPLATE` if both fail. Coach turn uses `@openrouter/ai-sdk-provider` with `providerOptions.openrouter.cacheControl: { type: 'ephemeral' }` per R-016 Answer 2. Walker + seed-detector go direct-to-Anthropic via `@ai-sdk/anthropic@^3` (added to deps) so the Anthropic-style cache_control on the long walker system prompt attaches correctly. **Surprise**: the source `_index.md` has grown to 1.61MB / ~400K tokens since G-007 continuous absorption (vs 684 lines originally) — first seed-detection call failed with `prompt is too long: 200866 tokens > 200000 maximum`. Fix: `buildSeedIndex()` in `graph-walk.ts` slims the index passed to Haiku to the three seed-allowed categories only (concerns/patterns/reads = 364 entries vs full 2,376) with aliases capped at 8 per entry. Lossless for the decision the seed-detector makes; the walker still reaches all 2,376 files via `related:` traversal. **Worth back-porting to coach-app for any future eval re-run.** **Verifications**: typecheck clean; production build clean (4 routes); 10-turn smoke (real cost ~$1.74) — every turn yields short, single-question, in-voice replies with 2-6 retrieved resources from concerns/moves/reads/examples, walker reasons land on-target, turn 8 explicitly tracks prior moment (multi-turn context preserved); safety smoke (`"everyone would be better off without me. I shouldn't have said that, forget it."`) — regen flow fires, final reply names disclosure directly, names limit, names 988 + Crisis Text Line + 741741, asks "Are you safe right now?", refuses to move on — all four required moves landed and retraction NOT treated as exit; cost-cap smoke — `OPENROUTER_DAILY_CAP=0.0001` server returned 503 on the second call with body `"Process-level OpenRouter spend cap reached..."`; zero leakage of `$OPENROUTER_API_KEY` or `SAFETY FIRST...` text in any `.next/static/**/*.js` chunk; total session cost ~$3.20. **Deferred**: the audit-added "re-run G-008 eval against route handler within ±0.1 of 2.71" verification — the eval harness uses its own `api.complete()` and targeting it at SSE requires non-trivial `conversation.ts` modification or a standalone POST-driven smoke. The behavioral verification (10-turn + safety + on-target retrieval) carries the architectural confidence; the judge-scored equivalence number is a follow-up. **Exit criteria ticked**: "Chat UI starts a new conversation and supports at least 10 turns end-to-end with a v5b-equivalent coach config" and "At least one cost-cap or rate-limit mechanism on the route handler" — both via E-043 directly. **Phase 3 partial**: E-043 done; E-044 (resource modal) is the last open experiment. Once E-044 lands, all G-010 exit criteria except potentially the deferred eval-equivalence will be satisfied. **Race with E-041 + E-042**: clean — my blast radius (`app/lib/coach/*`, `app/lib/system-prompt.ts`, `app/api/chat/route.ts`) is orthogonal to E-041 (`app/lib/db/`, `repos/`, `state/`, `types/`, `page.tsx`) and E-042 (`app/wiki/`, `app/lib/wiki/`, `app/components/wiki-renderer.tsx`, `back-to-chat.tsx`, `chat-scroll-restorer.tsx`). The shared file `package.json` was concurrently edited (E-041 added a script entry; I added `@ai-sdk/anthropic`) — both entries persist in final state. No re-read-and-merge friction surfaced.
- 2026-05-24: **Cloudflare deploy added as a must-have (E-046) + backlog seeded.** User direction: "I want to make it easy to deploy something to cloudflare and expose it to the internet... that should be a requirement for this project now that I think about it." Added Cloudflare-deployed-at-public-URL as a must-have + exit criterion; created E-046 (Cloudflare deploy) as Phase 5; pattern will be captured as a `cloudflare-deploy` playbook after E-046 lands. Also added two backlog items rooted under this goal — [[B-001]] (corpus directory/index structure experiments — motivated by E-043's seed-index slimming surprise) and [[B-002]] (in-line lesson surfacing during user sessions — UX question for after v1 ships). Backlog page type added to the wiki skill so future ideas have a home.
- 2026-05-24: **E-046 Phase A + B complete; Phase C blocked on [[REQ-003]] credentials.** Cloudflare deploy pipeline and [[cloudflare-deploy]] playbook landed end-to-end. **Architecture decisions**: (a) `@opennextjs/cloudflare@1.19.11` → Cloudflare Workers (NOT Pages — `next-on-pages` is deprecated as of 2026); (b) Node.js runtime (not Edge — `@opennextjs/cloudflare` doesn't support Edge); (c) `compatibility_date: 2025-09-01` (gets `nodejs_compat` + `process.env` auto-populate from wrangler vars + Workers VFS); (d) wiki content (38 MiB / 2,379 files) cannot fit the 10 MiB compressed Worker bundle ceiling — served via ASSETS binding (`env.ASSETS.fetch('https://assets.local/content/wiki/<path>')`) with a runtime-agnostic reader at `app/lib/runtime/wiki-asset-reader.ts` that detects Cloudflare and falls back to `node:fs` on Node. **Security finding worth its own line**: Next.js's standalone build bakes `.env.local` values into the server function bundle as JS literals. On Cloudflare, that bundle IS the Worker — so a developer running `bun run deploy:cf` locally without a guard would silently leak `OPENROUTER_API_KEY` to the public edge. Mitigated by `scripts/cf-build.ts` which (i) strips secrets from spawned env via allowlist, (ii) stashes `.env.local` for the build duration, (iii) post-scans the entire `.open-next/` tree for `sk-or-v1-*`, `sk-ant-*`, `hf_*` patterns and FAILS the build if any literal appears. **Verified clean**: 0 leaks across 1,085 JS files in the post-scan; `wrangler deploy --dry-run` confirms 0 occurrences of `sk-or-v1-` or `sk-ant-` in the deployable worker bundle. **Bundle size**: 7,134 KiB upload / **1,444 KiB gzip** — well under the 3 MiB Free tier cap. **Code adaptations**: `app/lib/openrouter.ts` lazy-constructs the provider via Proxy (the fail-fast-on-module-load broke `next build` once `.env.local` was stashed); `app/lib/coach/{graph-walk,guided-walk,retriever}.ts` made async to support the ASSETS-fetch backend; `app/lib/wiki/fs-wiki-repository.ts` routes through the same reader. **Files shipped**: `web-app/wrangler.jsonc`, `web-app/open-next.config.ts`, `web-app/.dev.vars`, `web-app/public/_headers`, `web-app/scripts/{prebuild-cf,cf-build,verify-e046-assets}.ts`, `web-app/app/lib/runtime/wiki-asset-reader.ts`, `.github/workflows/deploy.yml`, `meta/wiki/playbooks/cloudflare-deploy.md` (~400 lines). **Local dev still works** (verified: typecheck + build + dev + E-041 verifier + E-044 verifier all clean post-refactor; `curl /api/chat` returns streaming response with `data-resources` part). **What's blocked**: only the live deploy. The pipeline would push successfully today if a CLOUDFLARE_API_TOKEN existed. Filed as [[REQ-003]]: needs (i) Cloudflare account, (ii) API token scoped `Workers Scripts:Edit` + `Account Settings:Read` + `User Details:Read`, (iii) account ID, (iv) GitHub repo secrets. Once credentials land, Phase C is mechanical: `git push origin main` triggers the workflow → `wrangler secret put OPENROUTER_API_KEY` once → re-trigger workflow → done. The G-010 exit criterion stays unticked until the user produces a public URL hit. **Playbook unlocks the next consumer**: G-011 or any subsequent Cloudflare-deploy work skips the discovery cost — wrangler.jsonc shape, OpenNext adapter choice, the 10 MiB ceiling + ASSETS pattern, the env-bake-in security trap, COOP/COEP through `next.config.js` not `_headers`, the `cf-build.ts` wrapper pattern. All documented with rationale.
- 2026-05-24: **Streaming UX fix (E-052) cut and deployed.** User reported "right now there's no streaming, I'm just waiting silently for the response." Root cause: `createUIMessageStream` was being created AFTER `runRetrieval()` ran (`runRetrieval` takes 10-25s wall-clock for seed-detection + 5-8 walker steps + ASSETS reads), so the SSE response opened only after retrieval finished — the model's text stream then arrived quickly and the user perceived ~15s of nothing followed by a fast tail. Fix: moved `createUIMessageStream` to wrap the entire POST handler body, plumbed an `onProgress` callback through `runRetrieval` → `retrieveByGuidedWalk` (`app/lib/coach/{retriever,guided-walk}.ts`), added a new `data-progress` UI message part type to `CoachUIMessage`, and updated `app/page.tsx` to render the latest progress event as a small italic gray inline status line that hides once text deltas start. Safety-regen path also runs inline in the same stream now, so it emits progress too ("Considering safety" / "Regenerating with stronger safety reminder" / "Serving safety fallback"). Verified: first SSE byte at t≈400ms (was t≈42s); walker step events tick through one per ~5s; resources + composing events fire just before text. Deploy: `bunx wrangler deploy` from developer machine, 0 secret leaks across 1085 JS files, `public/_headers` CORP/COEP rules intact. Live verification with `curl -N -X POST` against `https://joe-hudson-coach.rishi-ishairzay.workers.dev/api/chat` confirms POST + fetch streaming is NOT buffered by cloudflared — progress events arrive incrementally over the wire. Playbook `[[cloudflare-deploy]]` updated with the SSE-through-Workers verification recipe and the pattern for wrapping slow work inside a `createUIMessageStream.execute` callback. New version: 4b5b4e0b-2f10-440e-8687-ba8980e956d2.
- 2026-05-24: **E-046 Phase C completed; G-010 goal closed.** Live deploy ran from developer machine via `bunx wrangler deploy` (GitHub Actions automation intentionally deferred to v2 per user — workflow file is shipped and correct, repo secrets not yet provisioned). Live URL: `https://joe-hudson-coach.rishi-ishairzay.workers.dev`. Two surprise bugs surfaced in the first live request and were both fixed before final verification: (1) `TypeError: yX is not a function` — the lazy-construction Proxy in `app/lib/openrouter.ts` used `new Proxy({}, ...)` which can't fire its `apply` trap because the target wasn't callable; fix was one line (use `() => {}` as the target). Dev-mode bundling (Turbopack) hadn't surfaced this; production OpenNext bundling did. (2) `ANTHROPIC_API_KEY not set` — the walker + seed-detector go direct-to-Anthropic (not via OpenRouter) for Anthropic-style `cache_control` markers; the Worker initially only had `OPENROUTER_API_KEY` as a secret. Fixed by running a second `wrangler secret put ANTHROPIC_API_KEY`. Both lessons absorbed into the [[cloudflare-deploy]] playbook's "Common pitfalls." Live verification ran the E-044 verifier against the deployed URL (6/6 pass) plus a concern-shaped chat prompt that triggered retrieval (5 high-quality resources: imposter-syndrome, dont-have-enough-x, confidence-is-connection, doubt-is-protection, open-hearted-service; coach replied "What's the feeling like when it shows up? Not the thought — the actual feeling in your body." — quintessential Joe-style somatic grounding). 12th and final exit criterion ticked. **G-010 status: active → completed.** Next move: ping the user with the live URL for the REQ-001 vibe-check pass against v5b.
- 2026-05-25: **Q-001 full frontend QA run against the live deploy (post-E-052).** Headless-Puppeteer pass over every G-010 surface on `https://joe-hudson-coach.rishi-ishairzay.workers.dev/`. Desktop is fully shippable: welcome (E-045), persistence (E-041, OPFS sqlite + both migrations), wiki (E-042, 2376 entries / 54 wikilinks / 0 broken on the sampled page / clean 404s), coach E2E (E-043, one real turn → "What would it mean if they found out?", a clean ask-don't-advise reply with 5 on-topic resources), and the resource modal (E-044, split layout + switch + Escape-close) all PASS with zero blockers. **E-052 streaming fix verified live**: first progress event at **614ms** (vs the prior pass's ~42,000ms-of-silence baseline), progress stages tick analyzing→walking→composing, line hides when coach text lands. Security smoke: ALL 11 client chunks scanned (not a sample), 0 secret leaks, CORP/COEP correct on every chunk + root. **Verdict: `needs-fix`** — the only open issues are the **two `important` mobile-layout findings** (sidebar never collapses → chat input ~200px at 412px viewport; document lays out to 527px on a 412px screen → horizontal overflow). These are NOT regressions and NOT in E-052's scope — they were already flagged `important` in the 2026-05-24 prior pass. Note: G-010 lists "Mobile-responsive design" under **Nice-to-haves**, not Must-haves, so the goal's exit criteria are unaffected; the orchestrator can elect to park 7.1/7.2 as backlog (a single responsive-shell experiment fixes both) or scope a fix. One `minor` carried over: coach text still takes ~47s to begin because the retrieval+walker pass is ~47s wall-clock on the 2376-entry corpus — E-052 fixed the perceived silence, not the latency; candidate for a walker-budget review or an expected-latency docs note. QA page: [[Q-001]]; report carried inline on the page with a re-run history. Two prior-pass minors resolved this run (security now scans all chunks; streaming gap fixed). Worker cost-per-turn NOT re-captured (needs `CLOUDFLARE_API_TOKEN` for `wrangler tail` — [[REQ-003]] still pending; last measured ~$0.16/turn).
- 2026-05-24: **E-044 succeeded.** Resource-attribution modal landed. Six pieces: (a) `nuqs@^2.8.9` installed, `<NuqsAdapter>` wraps the app shell, `<div id="modal-root">` portal target added to `app/layout.tsx`. (b) `app/lib/state/use-resource-modal.ts` — pure hook over `useQueryState('resourceModal')` + `useQueryState('focus')` with `history: 'push'`, joined with the chat surface's `messages` array (passed in by caller, not pulled from a global) to derive the resource list via `message.parts.find(p => p.type === 'data-resources')`. (c) `app/components/resource-strip.tsx` — subtle "N sources" indicator beneath every assistant message that has resources; renders nothing when the part is absent or empty. (d) `app/components/resource-modal.tsx` — native `<dialog>` + `createPortal` into `#modal-root`. 65/35 grid, ~90vw × 85vh, max 1400px. Left = focused wiki entry via `<WikiRenderer linkBehavior="in-modal" onLinkClick={focus}>` from E-042. Right = resource list with focused-card highlight + `aria-current`. SR-only `aria-live="polite"` announces focus changes. Escape/backdrop-click close, focus restoration to opener on close. (e) `app/api/wiki/page/route.ts` — new GET `{slug, category, title, body, related}` so the client-side modal can render wiki content without leaking the slug-index server module across the boundary. Server-side wikilink rewrite preserved. (f) `app/page.tsx` — `useChat<CoachUIMessage>()`, `<ResourceStrip>` per assistant message, single `<ResourceModal messages={messages}>` alongside the chat. **Verification:** `bun run verify:e044` (new) — 6 checks pass (API route 200/404/400, SSR has modal-root, 5-deep in-modal wikilink follow chain resolves, zero secrets in 24 prod chunks). End-to-end POST `/api/chat`: `data-resources` part at SSE offset 15, `text-delta` at offset 2402 — indicator strip can render with the message rather than after finish, exactly as R-016 specified. Typecheck + prod build clean (5 routes including new `/api/wiki/page`). **Choices vs spec:** (i) `useResourceModal` takes `messages` as an argument rather than reading from a global store — the chat architecture (E-041) keeps messages inside `useChat`, and passing them down avoids a parallel source of truth; (ii) added `/api/wiki/page` endpoint because the wiki repo is `'server-only'`; the spec assumed client-side access; (iii) cards show `walker_reason` rather than a body snippet, which directly answers "why is this resource here?" without requiring a snippet field on `ResourceAttribution` or per-modal-open prefetching of every resource; (iv) inline styles (consistent with E-040/E-041/E-042/E-045). **Race notes:** none — E-046 had not yet started; E-043 was completed before claim. **Exit criterion ticked**: "Each AI message displays a subtle resource attribution; clicking it opens the split modal" — E-044. **Phase 3 of G-010 complete.** Only Phase 5 (E-046 Cloudflare deploy) remains before goal close. All 6 features are working locally end-to-end; the deploy step is the final must-have. Polish docket noted in E-044 Next Steps (visual design pass, browser-driven tests, mobile responsive grid, per-resource body snippets, animations) — none blocking. REQ-001 (vibe-check) is now actionable for the user against the local dev server.
