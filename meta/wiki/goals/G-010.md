---
type: goal
id: G-010
status: active
created: 2026-05-23
parent: root
---

# Build web app exposing coach chat and wiki

## Outcome

A Next.js web app with two surfaces: (1) a multi-conversation chat interface where users talk to any of the G-009 coach models with full per-message resource attribution, and (2) a navigable wiki view of the `coach/` compendium. The chat and wiki share the same underlying ingested compendium files (one source of truth), so a "resource used" link in a chat message goes to the same wiki entry a user could navigate to manually. The app is locally-first (client-side SQLite for conversation history) with a stateless Next.js backend that owns the system prompt and the OpenRouter API key.

The output is a deployable Next.js app at `web-app/` (or similar) plus an ingestion script that mirrors and link-normalizes `coach/` into the app's local wiki directory.

## Correctness

### Must-haves

- **Stack**: Next.js (App Router) + TypeScript + Vercel AI SDK + OpenRouter as the LLM gateway. Per [[ai-web-app]] playbook.
- **Architecture**: Repository pattern (owns reads + writes for a domain) with constructor-injected composition per [[coding-architecture]] playbook. Zustand is a thin reactive layer over repositories; not a separate "Store" abstraction.
- **Chat surface**:
  - Multiple conversations per user (browser-scoped — no server-side user accounts in v1)
  - Each conversation has many messages; messages persist to **client-side SQLite** (e.g., wa-sqlite + OPFS, or sqlocal) — chosen empirically in R-015
  - Messages are stored at **full AI SDK fidelity**: text, tool calls, tool results, message parts, role, all of it. Symmetry on round-trip: client sends back exactly what AI SDK produced.
  - The **system prompt never reaches the client.** Lives in the Next.js route handler. Client sends messages; server prepends system prompt; server returns streamed response.
  - Chat is wired to the G-009 coach models (initially v5b Sonnet walker; future configs are pluggable). The route handler invokes the same retrieval pipeline as the eval-side coach.
- **Per-message resource attribution**:
  - Each AI message shows the resources (compendium files) used to produce it
  - Display is subtle (does not break conversational flow) — e.g., a small icon or chip count
  - Click → modal with split layout: **left** = full wiki entry for the focused resource, **right** = list of all resources used in that message
  - Clicking another resource in the right panel switches the left panel to that resource's wiki entry
  - "Back to chat" close action returns to the conversation
  - "Expand" action on the wiki entry opens it as a full page in the wiki view
- **Wiki surface**:
  - Navigable view of the ingested `coach/` content — every file is a viewable page
  - Wikilinks within entries (`[[some-file]]`, `[[category/some-file]]`) resolve and are clickable
  - Direct linking via URL slug (e.g., `/wiki/concepts/limiting-belief`)
- **Ingestion script** at `web-app/scripts/ingest-coach.ts` (or similar):
  - Copies `coach/` → `web-app/content/wiki/` (or similar)
  - Normalizes/fixes links so they all resolve in the web app (the `coach/` compendium has inconsistent link formats; the script fixes them)
  - Is **repeatable**: re-running it does not duplicate, does not lose customizations not in `coach/`, and reflects any changes to `coach/` since the last run
  - Is idempotent for the unchanged subset (so the second run with no `coach/` changes is a no-op)
- **Security**: `OPENROUTER_API_KEY` is server-only. No secrets bundled into client JS. System prompts are server-only.
- **No client account system in v1.** Conversations are browser-scoped. If two browsers visit the app, they're separate users.
- **First-visit welcome flow** explaining: (a) what the project is, (b) how it was built, (c) privacy posture (local-only chat storage; LLM provider as only network egress), (d) explicit unaffiliation with Joe Hudson / Art of Accomplishment, (e) brief mention that anonymous feedback may be requested later (no commitment to specific mechanism in v1). Short, dismissible, persists dismissal in localStorage, reachable later via help icon. No dedicated safety step in the welcome — runtime safety handling per [[E-031]] is trusted. See [[E-045]].

### Nice-to-haves

- Per-message cost display
- Export/import of a conversation
- Conversation sharing (URL with conversation state encoded)
- Anonymous feedback mechanism (e.g., 👍/👎 per assistant message + optional one-line comment, no account, no PII). Deferred from v1 per user direction on 2026-05-24; the welcome flow mentions it as a future possibility so the framing is in place for a clean future addition.
- Wiki search (full-text or keyword)
- Mobile-responsive design
- Streaming tool-call rendering (show the tool call appearing in the message in real time)
- Multiple coach configs selectable per conversation (e.g., "try this conversation with the Haiku walker instead")
- User account system (deferred to G-011+)

## Exit Criteria

- [ ] Next.js app runs locally with `cd web-app && bun run dev`
- [ ] Ingestion script `web-app/scripts/ingest-coach.ts` produces `web-app/content/wiki/` from `coach/` with all internal wikilinks resolvable
- [ ] Re-running the ingestion script after a `coach/` change reflects the change; re-running with no changes is a no-op
- [ ] Chat UI starts a new conversation and supports at least 10 turns end-to-end with a v5b-equivalent coach config
- [ ] Messages persist to client-side SQLite; refreshing the page reloads the conversation in full fidelity (tool calls intact)
- [ ] Server route handler invokes OpenRouter; no `OPENROUTER_API_KEY` is visible in network requests or client bundle
- [ ] System prompt is not visible in any network request from the client
- [ ] Each AI message displays a subtle resource attribution; clicking it opens the split modal (wiki entry left, resource list right)
- [ ] Wikilinks within a wiki entry resolve to the correct local page
- [ ] At least one cost-cap or rate-limit mechanism on the route handler so a runaway client cannot drain the OpenRouter budget
- [ ] First-visit welcome flow shows project intro, build summary, privacy posture, unaffiliation language; dismisses to localStorage; re-openable via help icon

## Context

G-009 produced the v5b coach (Sonnet walker over graph-walk retrieval, overall 2.71 on the G-008 eval). G-010 makes it usable — gives Rishi a way to actually talk to the coach interactively, and surfaces the compendium that powers it. This is the consumer of all prior work.

User-supplied constraints from the original transcript:
- Stack: Next.js + TS + AI SDK + OpenRouter (mandatory, captured in [[ai-web-app]] playbook)
- Client-side SQLite for chat history (full-fidelity AI SDK messages)
- System prompts server-only
- Single ingested copy of `coach/` shared by chat tools and wiki UI
- Subtle per-message resource attribution; modal-based deep dive
- Architecture: Repository pattern with composition; Zustand for reactive UI integration (captured in [[coding-architecture]] playbook; user revised on 2026-05-23 to drop the separate Store concept)

## Resources

- `OPENROUTER_API_KEY` (to be provided by user — see Resource Requests below) — server-only env var
- `ANTHROPIC_API_KEY` in `.env` is available but the playbook prefers OpenRouter routing for this app's stack
- `coach/` directory — source compendium (read-only from G-010's perspective; G-007 owns mutations)
- The G-009 retrieval pipeline lives at `coach-app/retrieval/` — the web app should adapt or reuse it (decision deferred to R-016)
- Playbooks: [[ai-web-app]], [[coding-architecture]]
- Estimated budget: TBD by user. Build cost is mostly local development (free); runtime/eval cost depends on use volume.

### Resource Requests

- [[REQ-002]] OpenRouter API key needed before any LLM-calling experiment can run

## Sub-goals

(none yet)

## Research

- [[R-015]] What's the best browser-side SQLite approach for full-fidelity AI SDK message persistence? — **pending**
- [[R-016]] How should the G-009 retrieval pipeline (graph-walk + Sonnet walker) be adapted from `coach-app/` to the web app? — **pending**
- [[R-017]] What's the canonical pattern for resource-attribution UX in chat (subtle in-flow indicator + modal deep-dive)? — **pending**
- [[R-018]] What link conventions exist in the current `coach/` directory and what normalization is needed? — **pending**

## Experiments

**Phase 1 — Foundation:**
- [[E-039]] Build repeatable ingestion script: copy `coach/` → web app content dir, normalize links — **pending**
- [[E-040]] Scaffold Next.js + AI SDK + OpenRouter minimal chat (no persistence, no wiki, no resource UI) — **pending**

**Phase 2 — Persistence & wiki:**
- [[E-041]] Add client-side SQLite for full-fidelity conversation persistence — **pending** (depends on E-041, R-015)
- [[E-042]] Build wiki view: routing, rendering, internal wikilinks — **pending** (depends on E-040)

**Phase 3 — Integration:**
- [[E-043]] Wire the v5b coach (graph-walk retrieval + Sonnet walker) into the chat route handler — **pending** (depends on E-041, R-016)
- [[E-044]] Build resource-attribution modal: split layout, navigate between resources, back-to-chat, expand-to-full — **pending** (depends on E-043, E-044, R-017)

**Phase 4 — Onboarding:**
- [[E-045]] Build first-visit welcome flow (project intro, build summary, privacy, unaffiliation) — **pending** (depends on E-040)

## Decision Log

- 2026-05-23: Goal created. Successor to G-009 (coach build). Makes the coach actually usable. Stack and architecture decided by user via questionnaire (App Router, OpenRouter always, interface+class repositories, Zustand). Two playbooks created to capture stack + architecture defaults: [[ai-web-app]] and [[coding-architecture]]. Goal is broken into 4 research items (browser SQLite, retrieval-pipeline adaptation, resource-attribution UX, link conventions) and 6 experiments across 3 phases (foundation → persistence/wiki → integration).
- 2026-05-23: **Coding-architecture playbook revised: dropped the separate "Store" concept.** User direction: Repository handles both reads and writes for a domain; no separate Store primitive. Composition pattern still applies. Zustand reframed in the playbook as a thin reactive layer over repositories rather than a Store abstraction. G-010 must-haves and experiment specs updated to remove Store references. Reason: the read/write split caused churn whenever something straddled the boundary; one Repository per domain is cleaner.
- 2026-05-23: **Coding-architecture playbook revised again: prefer few methods with rich param objects over many narrow methods.** Repository surface is now the four-method shape: `find(criteria)`, `create(input)`, `update(criteria, patch)`, `delete(criteria)`. The narrow alternative (`getById`, `getByEmail`, `updateName`, `deleteByOrgId`, etc.) is explicitly an anti-pattern. Reason: smaller surface per class, wrappers are dramatically simpler (4 methods to wrap, not 12+), adding a new filter dimension is an options-bag addition rather than a new method. E-041's ConversationRepository and MessageRepository specs updated. E-042's WikiRepository spec updated. Playbook example code rewritten to show the unified pattern + cache/log wrappers using it.
- 2026-05-23: **REQ-002 filed**: OpenRouter API key needed. G-010 cannot run any LLM-calling experiment without it. Existing `ANTHROPIC_API_KEY` could be a fallback if OpenRouter integration is blocked, but the playbook makes OpenRouter mandatory for this stack — going direct-to-Anthropic would diverge from the playbook and complicate later model swaps.
- 2026-05-23: **Local-first, no server accounts in v1.** Conversations live in browser-side SQLite per the user transcript. This makes deployment trivial (stateless backend), defers the auth question entirely, and lets the user demo by sending a URL. Trade-off: no cross-device sync. Acceptable for v1; future goal can layer in optional server-side sync.
- 2026-05-23: **Full-fidelity message persistence is non-negotiable per the transcript.** Storing only text would break tool-call round-tripping. The AI SDK's `UIMessage` type is the persistence unit — store the entire structured message, not a flattened text representation.
- 2026-05-23: **Single ingested copy of `coach/`** rather than two parallel reads. The chat's tools and the wiki's routes both read from `web-app/content/wiki/`. Ingestion script is the only place that touches `coach/`. This keeps tools and wiki guaranteed-consistent and makes link-normalization a one-time-per-ingest cost.
- 2026-05-23: **Audit performed** ([[G-010-audit]]). Critical findings: (1) E-043 is dramatically under-specified — the eval-side glue between retrieval and the coach call (`eval/lib/conversation.ts:270-352`, `eval/lib/retrieval-adapter.ts:122-181`) is NOT present in `coach-app/run.ts` and must be replicated by E-043; OpenRouter cache semantics for the Anthropic `cacheControl` markers used in `coach-app/lib/anthropic-client.ts` are unaddressed. (2) E-041 is partially-revised: interfaces follow the new four-method shape but hypothesis, step 6, and blast-radius still reference the old narrow methods and the dropped "Repository/Store" split. (3) E-039's corpus-size estimate of ~470 files is wrong by ~4x — actual is ~1,856 markdown files in the 14 standard categories (R-014 reported ~1,059 mid-month; G-007 absorption continues). (4) Leftover architectural-Store references in R-015, E-040, E-041 contradict the playbook revision. Important: no equivalence test that web-app v5b ≈ eval-measured v5b; full-fidelity persistence success criterion is too soft to catch silent drops; cost-cap mechanism is hand-waved given no-auth v1; R-016 doesn't mandate an answer on AI SDK message-annotation mechanism for telemetry transport; E-044 references partial routing-file syntax as if R-017 has decided. Recommendations docket queued for `wiki-next`.
- 2026-05-23: **Audit resolved.** All 4 critical findings + most important findings addressed. Key changes: (1) E-043's Hypothesis + Method rewritten to reflect that the retrieval-to-coach orchestration lives in the EVAL harness (`eval/lib/conversation.ts`, `eval/lib/retrieval-adapter.ts`), NOT in `coach-app/run.ts` — porting the v5b coach is not a small adapter job. Method now separates "port" (walker, graph, safety) from "new orchestration code" (trigger policy, injection, history, retry, cost-cap). (2) R-016 restructured to enumerate four MUST-answers before E-043 can start: orchestration port location, prompt-cache markers under OpenRouter (`providerOptions.anthropic.cacheControl` won't propagate through `@ai-sdk/openai`), AI SDK message-annotation transport for telemetry, adaptation strategy comparison. (3) E-043 adds a behavioral verification step: re-run G-008 eval against the web-app route handler as coach target; mean per-dim must land within ±0.1 of E-038's 2.71. (4) E-043 cost cap concretised as process-level circuit-breaker (`OPENROUTER_DAILY_CAP=5` USD default, 503 on exceed) — per-IP/per-user limits are auth-gated and v2. (5) E-039 corpus count corrected (1860 not 470), integrity threshold revised to 90%, ingestion coordination with `rebuild-index.sh` flagged, scope clarified that output dir is mirror-only. (6) E-041 Repository/Store leftovers and old narrow-method references cleaned up; success criterion tightened to byte-equality of UIMessage JSON round-trip. (7) R-018 explicitly handles Obsidian alias syntax `[[slug|display]]`. (8) `ai-web-app.md` useChat import path corrected from `ai/react` to `@ai-sdk/react`. Audit status moved to `addressed`; see `meta/wiki/audits/G-010-audit.md` Resolution section for the per-finding accounting.
- 2026-05-24: **OpenRouter key landed; REQ-002 resolved.** User provided `OPENROUTER_API_KEY` and `HF_TOKEN`; both stashed in `.env` (gitignored). E-040 and E-043 are unblocked on credentials. R-016 (the four MUST-answers) remains the next research priority before E-043 can actually run.
- 2026-05-24: **Welcome flow added as E-045 (Phase 4).** User direction via the goal command: short and sweet first-visit modal explaining what the project is, roughly how it was built, privacy (local-only, LLM provider is only egress), and explicit unaffiliation with Joe Hudson / Art of Accomplishment. Decisions via questionnaire: (a) unaffiliated framing explicit, (b) NO dedicated safety step in welcome (trust runtime safety per E-031), (c) anonymous feedback mechanism DEFERRED — welcome mentions it may be requested later but commits to no specific UI in v1; added as nice-to-have for a future experiment, (d) first-visit-only, dismissible to localStorage, re-openable via help icon. Welcome content spec'd in E-045 across 4 sections (what, how, privacy, get started) with example seed prompts that mirror REQ-001's vibe-check suggestions.
