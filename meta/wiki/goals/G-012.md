---
type: goal
id: G-012
status: active
created: 2026-05-24
parent: root
---

# Hetzner personal-PaaS: project.play.ris.hair routing via Cloudflare Tunnel with Cloudflare Access

## Outcome

A lightweight personal-PaaS on the Hetzner box that lets any project running on a local port be exposed at `<project>.play.ris.hair` with Cloudflare Access auth, no public Hetzner ports, and `systemd` keeping processes alive across reboots. Adding a new project takes one command and a tiny YAML file.

Concretely: a CLI tool (`./paas register|up|down|list|logs`) plus one Cloudflare Tunnel, one Cloudflare Access policy, and per-project `systemd` units generated from each project's `.local-dev.yaml`.

The output is a script + small directory of configs that turns "I want this dashboard reachable from my phone" into a 30-second operation.

## Correctness

### Must-haves

- **Subdomain pattern**: each project lives at `<project>.play.ris.hair` (sub-subdomain — each project independent, easy to promote to root subdomain on Cloudflare Pages later).
- **One Cloudflare Tunnel** runs as a `systemd` service on the Hetzner box, exposes local ports to Cloudflare's network. No Hetzner public ports opened for the tunnel.
- **Cloudflare Access** policy applied at the tunnel: only the user's email(s) can reach any `*.play.ris.hair` subdomain. Anyone else gets the CF Access login page and bounces.
- **Per-project `.local-dev.yaml`** (in the project's repo root) declaring at minimum: `subdomain`, `port`, `command` (e.g., `bun run dev` or `bun run start`), `mode` (`dev` or `built`). Optionally: env vars, restart policy.
- **CLI tool** at `~/bin/paas` (or similar) with subcommands:
  - `paas register <path>` — reads the project's `.local-dev.yaml`, generates a `systemd` unit, updates the tunnel config, restarts the tunnel
  - `paas up <project>` / `paas down <project>` — start/stop the systemd unit
  - `paas list` — what's registered, what's running, what's the subdomain
  - `paas logs <project>` — tail `journalctl` for that unit
  - `paas remove <project>` — tear down (systemd unit, tunnel route, etc.)
- **Process supervision via `systemd` user units** (`systemctl --user`) so projects survive reboots and auto-restart on crash. No `tmux`, no `pm2` (one less moving part).
- **Tunnel config is generated, not hand-edited.** The CLI maintains `~/.cloudflared/config.yml` (or equivalent) as a derived artifact of registered projects.
- **Secret handling**: project env vars come from each project's `.env` (gitignored), referenced by the systemd unit's `EnvironmentFile=`. CLI never writes secrets into systemd units directly.
- **The dogfood**: [[G-013]] (status dashboard) is the first user. If G-012 can't host G-013 cleanly, G-012 isn't done.

### Nice-to-haves

- ~~A simple "what's running" status page at `paas.play.ris.hair`~~ — **deferred to v2 (audit rec #10)**. [[G-013]]'s status dashboard already covers the "what's running" view as part of its wiki-state section; building a separate `paas.play.ris.hair` page would duplicate that surface. If a paas-specific operational view is wanted later, add it as a section in G-013's dashboard rather than a separate project.
- Per-project resource limits (memory cap via systemd) — **shipped as a default in v1** per audit rec #15: every generated systemd unit includes `MemoryMax=512M`, overridable via `.local-dev.yaml`'s `memory_max` field. Single runaway Bun process can no longer OOM the box.
- Restart-on-config-change (CLI watches `.local-dev.yaml` and re-applies) — defer; `paas restart <name>` is the manual primitive in v1.
- Promotion path: `paas promote <project>` outputs the steps to deploy this project to Cloudflare Pages (the production graduation) — defer to v2.
- `paas register --dry-run` (audit rec #16): shipped in v1 because it's small, valuable for first-time use, and prevents the worst class of "I didn't realize that would happen" surprises.

## Exit Criteria

- [ ] `paas` CLI exists at a known path; subcommands (`register`, `up`, `down`, `restart`, `list`, `logs`, `remove`) all work
- [ ] One project (G-013's status dashboard) is registered and reachable at its `*.play.ris.hair` URL — closed by [[E-051]] (the merged dogfood deployment experiment; see decision log 2026-05-24)
- [ ] Cloudflare Tunnel is running as a `systemd` service that survives a Hetzner reboot (verified by an actual `systemctl reboot` or `systemctl --user`-cycle simulation in E-047; per-user services come back via `loginctl enable-linger`)
- [ ] Cloudflare Access blocks unauthenticated requests to `*.play.ris.hair` and lets the user through after Google/OTP SSO
- [ ] `paas list` shows the project, its port, its subdomain, its current systemd status
- [ ] `journalctl --user -u <unit>` works for project logs (CLI's `logs` subcommand wraps this)
- [ ] Project's `systemd` unit auto-restarts on crash (`Restart=always`, `RestartSec=2s`)
- [ ] Documentation in `paas/README.md` explains the per-project `.local-dev.yaml` format and the "add a new project" workflow, plus the brief 2-30s tunnel reconnect on each `paas register`

## Context

The user is running multiple personal projects on the Hetzner box (this project's status dashboard, the OpenRouter model registry dashboard, future tools). Each needs to be reachable from any device the user owns, with auth so it's not publicly browsable. Doing this ad-hoc per-project (manually configuring nginx, manually managing TLS, manually starting tmux sessions) doesn't scale.

User-stated framing: **Hetzner = personal/play, Cloudflare = public/production.** The dividing line is "are other people going to use it." G-012 is the Hetzner side of that split. The Cloudflare side is per-project: G-010's [[E-046]] handles the coach's Cloudflare deploy; future public projects will get their own production-deploy experiments.

User design choices recorded in conversation 2026-05-24:
- **Sub-subdomain per project** (`<project>.play.ris.hair`), not path-based — keeps projects independent and lets them graduate to a root subdomain on Cloudflare Pages without code changes.
- **Cloudflare Tunnel**, not Caddy/nginx with exposed ports — no exposed Hetzner ports, automatic TLS, consistent operational surface with the Cloudflare-public side.
- **Cloudflare Access**, not security-by-obscurity — apps don't implement auth; CF Access blocks unauthenticated traffic at the edge.

## Resources

- Hetzner box (this machine) — already provisioned
- `ris.hair` domain — presumably already on Cloudflare DNS (need to verify; if not, that's a REQ)
- Cloudflare account (free tier covers Tunnel + Access for ≤50 users; ACM at $10/mo/zone may be needed for two-level wildcard SSL — see decision log)
- `OPENROUTER_API_KEY` and `HF_TOKEN` already in `.env` (project-side env handling will reference per-project `.env` files)

### Resource Requests

- [[REQ-004]] Cloudflare Tunnel + Access dashboard prerequisites and API token for G-012 PaaS — covers Tunnel + DNS + Access scope, SSL decision (ACM vs flattened scheme), IDP setup, and Zero Trust activation. (REQ-003 covers G-010's Workers deploy and does NOT cover G-012's needs — distinct token scope and product surface.)

## Sub-goals

(none yet)

## Research

(initial research items to be created by the audit cycle)

## Experiments

(initial experiments to be created by the audit cycle)

## Decision Log

- 2026-05-24: Goal created. Motivated by the user's distinction between Hetzner-for-personal-play vs Cloudflare-for-public-production. Adding the Nth project to the personal-play side should be a 30-second operation, not an afternoon of nginx config and TLS wrangling. Sub-subdomain pattern + Cloudflare Tunnel + Cloudflare Access chosen for the reasons above. systemd user units for process supervision (one less tool than pm2 or tmux). G-013 (status dashboard) is the first dogfood user; if G-012 can't host G-013, G-012 isn't done.
- 2026-05-24: Audit performed (see [[G-012-audit]]). Key findings: (1) **Critical:** systemd `--user` units need `loginctl enable-linger` for boot survival — currently unaddressed and breaks the "survives reboots" exit criterion. (2) **Critical:** `paas register` workflow has unspecified atomicity (concurrent invocations race on `~/.cloudflared/config.yml`, partial-crash leaves orphan state, `systemctl restart` is wrong scope for system `cloudflared` from a user CLI). (3) **Critical:** R-019 forecloses alternatives comparison — Tailscale Funnel/Serve in particular may better fit the user's "Hetzner=personal, Cloudflare=public" framing and should at minimum be surveyed. (4) **Important:** E-049 and E-051 are co-completing/duplicated experiments and should be merged. (5) **Important:** `.local-dev.yaml` schema has silent gaps (port conflicts, env_file path resolution, `paas restart` missing for env reloads). (6) **Important:** REQ-003 is reused but covers G-010's needs, not G-012's Tunnel/Access dashboard prerequisites. Audit recommendations queued for `wiki-next` to address.
- 2026-05-24: Audit recommendations addressed. (1) Linger requirement now documented in R-019 ("Boot survival" section) and added as the first method step in E-047 plus a reboot-test success criterion. (2) Atomicity mechanism specified in R-019 (`flock` + tmpfile + validate + rename + restart with poll) and E-048 updated accordingly. Confirmed via R-019 that cloudflared has NO SIGHUP-reload as of May 2026, so `sudo systemctl restart cloudflared` is the canonical reload primitive — the "sub-second downtime" claim in E-048 was incorrect and is now updated to "2-30s tunnel reconnect, in-flight long-lived connections dropped." Sudoers/polkit setup specified (passwordless `systemctl restart cloudflared` for the user; symlink workaround so config writes don't need sudo). (3) R-019 expanded to include alternatives comparison: Tailscale Funnel (disqualified — no custom domain), Tailscale Serve (disqualified — private tailnet only, no subdomain story), Caddy + Cloudflare DNS-01 (disqualified — requires public 443), Pomerium/Authelia (disqualified — same), wireguard+Caddy (loses public addressability). Cloudflare Tunnel + Access remains the right primitive because it's the only one satisfying the full conjunction of {custom subdomain pattern, no public Hetzner ports, IDP-backed auth, single-config-file model}. (4) E-049 abandoned; [[E-051]] is the single closer for both G-012 and G-013 exit criteria. (5) `.local-dev.yaml` schema tightened in E-048: port-conflict detection, `env_file` relative-to-working_dir resolution, `paas restart` added as a subcommand, secrets stay in `.env` (CLI never embeds in unit), `mode` field's role made concrete (sets `NODE_ENV` and the `command` default). (6) New [[REQ-004]] filed covering G-012's specific dashboard work (Tunnel + DNS + Access scope, Zero Trust activation, IDP setup, SSL decision); REQ-003 stays scoped to G-010's Workers deploy. E-047's `depends_on` now points to REQ-004. R-019's findings also surfaced an additional decision: **SSL coverage for `*.play.ris.hair` (two-level wildcard) is NOT covered by free Universal SSL.** Options surfaced: (A) Advanced Certificate Manager at $10/mo/zone, (B) flatten subdomain scheme to `<project>-play.ris.hair`, (C) per-subdomain explicit cert.
- 2026-05-24: **SSL decision (user):** **(C) per-subdomain explicit cert.** No wildcard, no recurring cost. URLs stay clean as `<project>.play.ris.hair`. `paas register` now owns creating the CNAME for each new subdomain (`<project>.play → <tunnel-uuid>.cfargotunnel.com`, proxied) and polling cert issuance to `active` (10-30s typical) before reporting success. `paas remove` deletes the DNS record (cert auto-revokes). REQ-004's required token scopes updated to add `Zone > DNS > Edit` and `Zone > SSL and Certificates > Edit`. E-048 method updated with the DNS+cert provisioning step. Rejected: ACM ($120/yr not justified for personal scope); flatten-naming (loses sub-subdomain clarity, complicates Cloudflare Pages graduation).
